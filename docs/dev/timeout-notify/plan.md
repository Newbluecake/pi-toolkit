# 超时宽限与续期（timeout grace & extension）实施方案 · v2

> 状态：实施方案 **v2** —— 已按评审 v1 修订（`review.md`），用户已拍板"修订后直接开工"
> 上游：`docs/dev/timeout-notify/arch.md`（架构设计稿 v2）、`docs/dev/timeout-notify/review.md`、`docs/dev/timeout-notify/explore.md`
> 约束：`AGENTS.md`（zero-hang 不变量、matrix/属性测试 lockstep、无模块级可变状态、timer unref、ESM `.js` 后缀、typebox、Conventional Commits）
> 本文只描述"怎么做、谁做、按什么顺序、怎么验"，设计理由一律引用 arch.md 章节号 / 决策号（D-1 ~ D-17），不重复论证。
> 四项已确认决策不动：宽限续跑（默认可配）→ 通知注入主会话上下文 → `extend_subagent_timeout` → 延长双上限（次数 + `hardDeadlineAt`）。
>
> **v2 相对 v1 的结构性变化**：① 新增冻结面文件 `agent-tool.ts` / `workflow-tool.ts`（`timeout_ms → timeout_s`，D-12）、`core/status.ts`（D-15）；② 砍掉 RPC caps 工作项与用例（BL-2）；③ `spawn-service.ts` 入 P-final（D-10/D-16 的 `applyBudgetPolicy` 调用点 + BL-5 终态重建）；④ 三个无 owner 测试文件补进文件域（RK-11）；⑤ Pkg B 去掉 `it.skip` 跨包解耦（RK-10）；⑥ 验收矩阵 V1–V18 → V1–V24；⑦ 提交 8 → 9（P0 拆出一枚 breaking 的重命名 commit）。

---

## 0. 源码接缝核实表（施工前逐条读码复核；行号一律改为符号引用，RK-13）

| #   | 断言                                                                                                                                                                                                                                                    | 位置（符号）                                                                                                                                                                                                                                                                                                                                                                                                                                                         | 核实结论 / 对施工的影响                                                                                                                                                                                                                                                                                                                                                                                                   |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| S1  | `deadlineAt` 唯一写入点是 `enqueued` 分支；`clearAndArm()` 把相位 timer 的 `arm_timer.dueAt` 写成 `min(phaseDue, deadlineAt)`                                                                                                                           | `src/core/state-machine.ts` `reduce()` 的 `input.kind === "enqueued"` 分支；`clearAndArm()`                                                                                                                                                                                                                                                                                                                                                                          | **文本真、因果假（RK-1）**：`arm_timer` effect 无消费者（`BasicEffectInterpreter` 未注册 handler），watchdog 对非总类 timer 用 `dueAtFor` 现算 ⇒ "过去时上界误触发（R-2）"不是真实隐患。`rearmTimers` 仍要做，承重点改为"宽限中 `armedTimers` 只含 `total_grace`、不含 `total`"（arch §3.4）                                                                                                                              |
| S2  | watchdog `tick()` 对 `total` 直接读 `state.deadlines.deadlineAt`，其它 timer 走 `dueAtFor(phase, diag, this.deps.budget)`（**不**与 `deadlineAt` 取 min）；`deps.budget` 是**全局** settings.budget，不是 per-run                                       | `src/runtime/watchdog.ts` `EventWatchdog.tick()`                                                                                                                                                                                                                                                                                                                                                                                                                     | 真。`total_grace` 同样直接读 `graceUntil`，不经过 budget，per-run 覆盖无歧义                                                                                                                                                                                                                                                                                                                                              |
| S3  | runner 的 prompt guard 是一次性 `clock.setTimer(remainingFor(budget.totalMs,…).ms)`；`totalMs === 0` 时 `remainingFor` 返回 `{ms:0, capped:"phase"}` → `setTimer(0)` → prompt 立刻假超时                                                                | `src/runtime/runner.ts` `run()` 内 `promptBudget` / `this.guard(handle.prompt(...))`；`src/core/deadline.ts` `remainingFor()`                                                                                                                                                                                                                                                                                                                                        | **真，但 v1 的修复路径错（BL-1）**：`clearAndArm` 在 `totalMs === 0` 时不武装任何 timer（含子阶段），修掉假超时 = 永久挂死。**改为 D-11**：配置层禁止 `totalMs ≤ 0`；`guardUntil` 的 `undefined` 分支只作防御。CHANGELOG 条目从 "Fixed: totalS=0 假超时" 改为 "Changed: `budget.totalS` 必须 > 0，0 回退默认并 WARN"                                                                                                      |
| S4  | `RuntimeRunner` 不在 `run()` 闭包外保存 per-run budget（`dispatchers` 条目只有 `{gen, fn}`）                                                                                                                                                            | `src/runtime/runner.ts` 类字段 `dispatchers`；`run()` 内 `this.dispatchers.set(req.runId, { gen, fn: dispatch })`                                                                                                                                                                                                                                                                                                                                                    | 真。同步 `extendDeadline()` 需要 budget 做 `extendability` 判定 → `dispatchers` 条目扩为 `{gen, fn, budget}`（与 `activeCancels/activeHandles` 同生命周期，`finally` 清理）                                                                                                                                                                                                                                               |
| S5  | `diag.autoBackgroundedAt` 只写在 spawn-service 自己的 `records` 副本上，**runner 的 `RunState.diag` 里没有**                                                                                                                                            | `src/service/spawn-service.ts` 终态重建处对 `autoBackgroundedAt` 的三重兜底                                                                                                                                                                                                                                                                                                                                                                                          | 真。caller-ack 抑制不能在 runtime-adapter 内自足实现：adapter 只做 CC2（它独占 `childRunIds`），规则 2/3 作为纯函数 `shouldDeliverDeadlineNotice()` 放 `src/delivery/deadline-notice.ts`，由 `stack.ts` 注入 `expectsAck`/`autoBackgrounded` 谓词后调用                                                                                                                                                                   |
| S6  | `DEFAULT_BUDGET` 的键驱动 `TIME_SETTING_MS_PATHS`（`*Ms` 后缀自动）与 `BUDGET_SPECS`（除 `startupRetries` 外一律 `seconds()`，`seconds()` 硬编码 `min: 0`）；`BUDGET_DESCRIPTIONS: Record<keyof DeadlineBudget,string>` 编译期强制补齐                  | `src/config/settings.ts` `TIME_SETTING_MS_PATHS`；`src/config/setting-specs.ts` `seconds()` / `BUDGET_DESCRIPTIONS` / `BUDGET_SPECS`                                                                                                                                                                                                                                                                                                                                 | 真。`DeadlineBudget` +3 字段与 `setting-specs.ts` **必须同一 commit**；`BUDGET_SPECS` 生成器白名单要扩（`maxExtensions` → count、`maxTotalFactor` → 非整数 number、`totalMs` → `seconds({ min: 1 })`，故 `seconds()` 需增 `min?` 选项）                                                                                                                                                                                   |
| S7  | `tsconfig.json` 只 include `src/**`；vitest 用 esbuild 不做类型检查                                                                                                                                                                                     | `tsconfig.json`、`vitest.config.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                  | 真。`INPUT_KINDS` +1 与 MATRIX 新列必须同一 commit（Pkg A）                                                                                                                                                                                                                                                                                                                                                               |
| S8  | `QueryService.wait()` 的动态默认等待 = `deadlineAt - now + WAIT_SETTLEMENT_GRACE_MS(30s)`；`stack.ts` 静态兜底 `defaultWaitMs = totalMs + abortGraceMs + 30s`                                                                                           | `src/service/query-service.ts` `wait()`；`src/stack.ts` `createQueryService({ defaultWaitMs })`                                                                                                                                                                                                                                                                                                                                                                      | 真。**RK-5 口径**：仅当 `snapshot.diag.overtime !== undefined` 才用 `hardDeadlineAt ?? deadlineAt`，否则维持 `deadlineAt`；静态兜底数值不变、补一句注释。读码补充：仓内唯一裸 `wait()` 调用方是 `index.ts forwardQuery`（`result-tool` 恒传 `waitMs = wait_ms ?? DEFAULT_WAIT_MS`），副作用面极小                                                                                                                         |
| S9  | ~~RPC `clampBudget` 对无 cap 的 budget 键放行到 `MAX_SAFE_INTEGER`，远端可把 `maxTotalFactor` 设成天文数字~~                                                                                                                                            | `src/rpc/protocol.ts` `SpawnParams.budgetOverride`；`src/rpc/server.ts` `onRequest` → `isSchema(RPCRequestSchema)`                                                                                                                                                                                                                                                                                                                                                   | **假（BL-2）**：`budgetOverride` 是逐键白名单 `Type.Object({...}, { additionalProperties: false })`，`server.ts` 先整包校验再 `clampBudget` ⇒ 远端根本发不进 `totalGraceMs`/`maxExtensions`/`maxTotalFactor`。**砍掉** DEFAULT_CAPS +3 与 rpc 用例。`server.ts` 仅改一处注释：`Math.max(1, bounded)` 的理由从 "0 disables the deadline" 改为引用 D-11（"0 现在回退宿主默认，可能大于 caps.totalMs；钳 ≥1 保住 cap 语义"） |
| S10 | `sendFormatted` 单条分支已 `store.get(payload.runId)` 取终态快照做兜底                                                                                                                                                                                  | `src/stack.ts` `sendFormatted`                                                                                                                                                                                                                                                                                                                                                                                                                                       | 真。arch §5.6 "finished in overtime" 尾巴挂在这里，读 `snapshot.diag.overtime`                                                                                                                                                                                                                                                                                                                                            |
| S11 | `createNotificationReceiptHook` 只认 `customType === "subagent:notification"`                                                                                                                                                                           | `src/stack.ts` `createNotificationReceiptHook`                                                                                                                                                                                                                                                                                                                                                                                                                       | 真。新通道 `subagent:timeout` 天然不被 receipt 记账，但要有一条集成断言锁死（arch §5.2）                                                                                                                                                                                                                                                                                                                                  |
| S12 | `RESERVED_TOOL_NAMES` 含 `set_model`（注释：host 守卫已隔离，加入是防 MCP 同名）                                                                                                                                                                        | `src/runtime/tool-scope.ts` `RESERVED_TOOL_NAMES`                                                                                                                                                                                                                                                                                                                                                                                                                    | 真。沿先例把 `extend_subagent_timeout` 加入（1 行 + tool-scope 测试）                                                                                                                                                                                                                                                                                                                                                     |
| S13 | `systemClock.setTimer` 已 `unref()`                                                                                                                                                                                                                     | `src/core/clock.ts` `systemClock`                                                                                                                                                                                                                                                                                                                                                                                                                                    | 真。`guardUntil` 重装 timer 复用同一 Clock，print 模式无新增常驻 timer（R-12）                                                                                                                                                                                                                                                                                                                                            |
| S14 | `formatDuration` 在 `src/core/format.ts`（core 层，零外部依赖）                                                                                                                                                                                         | —                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | 真。通知文案/工具文案/widget 一律用它                                                                                                                                                                                                                                                                                                                                                                                     |
| S15 | arch §5.3 样例文案需要 `turns/$cost/idle/currentTool` 与建议值 `min(totalMs, headroom)`                                                                                                                                                                 | arch §3.2、§5.3                                                                                                                                                                                                                                                                                                                                                                                                                                                      | 真。`DeadlineNotice.suggestedExtendMs`（core 算好，毫秒）；`turns/usage/currentTool/lastEventAt` 由 `formatDeadlineNotice(notice, { now, snapshot? })` 从 `query.get(runId)` 实时快照读取；文案把毫秒换成 `extend_s` 秒值                                                                                                                                                                                                 |
| S16 | `deadline.ts` 不能 import `state-machine.ts`（后者 import 前者）；今天终态判定散落多份                                                                                                                                                                  | `state-machine.ts` `terminal()`；`runner.ts` `TERMINAL_STATUSES`；另有 `stack.ts`/`index.ts`/`commands/status.ts`/`mention/mention.ts`/`core/delivery-key.ts`/`ui/fleet-panel.ts`                                                                                                                                                                                                                                                                                    | 真，但 v1 "在 `deadline.ts` 内联一份 Set" 是第三份拷贝（RK-6）。**改 D-15**：新建 `src/core/status.ts`，`state-machine.ts`/`runner.ts`/`deadline.ts` 三处统一；其余拷贝留后续 chore                                                                                                                                                                                                                                       |
| S17 | `timeout_ms` 全仓引用面（BL-4 改名波及）                                                                                                                                                                                                                | `src/tools/agent-tool.ts`（schema 字段 + `budgetOverride` 映射，2 处）；`src/tools/workflow-tool.ts`（schema、文件头注释、`promptSnippet`、`renderCall`、`execute` 的 `mergeBudget(deps.defaultBudget, params.timeout_ms)`，5 处）；`tests/tools/agent-tool.test.ts`（describe 名 + 用例名 + 参数，3 处）；`README.md` / `README.en.md`（`Agent` 工具参数行各 1 处）；`docs/dev/auto-background/plan.md`、`docs/dev/timeout-notify/explore.md`（历史记录，**不改**） | **源码 2 文件、测试 1 文件、README 2 文件**，共 5 个待改文件、12 处引用。`result-tool.ts`/`bash-job-tool.ts` 的 `wait_ms` 不在本次范围（§7）                                                                                                                                                                                                                                                                              |
| S18 | spawn-service 终态重建把 `deadlines` 缩成 `{ enqueuedAt, deadlineAt: diag.deadlineAt, queueDeadlineAt: undefined }`                                                                                                                                     | `src/service/spawn-service.ts` `outcomes.set(...)` 之后的 `snapshot` 组装                                                                                                                                                                                                                                                                                                                                                                                            | 真（BL-5）。必须补 `hardDeadlineAt: diag.hardDeadlineAt`、`graceUntil: diag.overtime?.grace?.until`——这正是 arch §3.2 给 `diag` 加两个镜像字段的原因。文件归 P-final                                                                                                                                                                                                                                                      |
| S19 | `mergeBudget()` 是**唯一**预算合并入口（调用点：`loadSettings`、`mergeSettings`、`spawn-service.spawn`），今天不做任何值校验；agent-type frontmatter 解析器（`config/agent-types.ts`）**不读 budget 键**，`AgentTypeConfig.budgetOverride` 只能程序设置 | `src/config/settings.ts` `mergeBudget`；`src/config/agent-types.ts` `parseAgentFile`                                                                                                                                                                                                                                                                                                                                                                                 | D-11 的规范化放 `mergeBudget`（丢弃非法层 → 回退下一层）即可覆盖 settings/frontmatter/per-spawn 三路；D-16 的合并后钳位仍做（防御 + 为将来 frontmatter budget 支持预留）                                                                                                                                                                                                                                                  |
| S20 | 既有测试对 `totalS: 0` / `totalMs: 0` 的依赖                                                                                                                                                                                                            | `tests/config/settings-time-migration.test.ts`（两处 fixture 期望 `totalMs === 0` 且 `warn` 未调用）；`tests/config/time-units.test.ts`（纯单位转换）；`tests/core/core.test.ts` "honors zero-budget disabling"（reducer 级）；`tests/rpc/rpc.test.ts`（`totalMs: 0` 已被 clamp 到 1）                                                                                                                                                                               | 前者 fixture 改为正值（0 的行为迁到新测试 `extend-settings.test.ts`）；其余三处**不受影响**（转换层不规范化、reducer 防御分支保留、RPC 已 clamp）                                                                                                                                                                                                                                                                         |
| S21 | 既有测试里 `{...DEFAULT_BUDGET, totalMs: 小值}` 且依赖 `total` 到点即 `timed_out` 的基线                                                                                                                                                                | `tests/core/core.test.ts` 顶部 `budget`；`tests/runtime/runtime.test.ts` 模块级 `budget`（`totalMs: 30`，prompt 永不 resolve 用例期望 t=31 `timed_out`）；`tests/integration/wiring.test.ts` `fastBudget`；`tests/service/deadline-cap.test.ts` `fastBudget`（有 cap，实际不受影响，防御性同步）                                                                                                                                                                     | 默认 `totalGraceMs = 90s` 一旦被 Pkg A 的 reducer 读取，这些 run 会进宽限而非 `timed_out` ⇒ **P0 预先给这四处基线加 `totalGraceMs: 0`**（P0 时 reducer 尚未读该字段，行为零变化；A 合入后基线不变）。宽限行为只在新 describe 里用显式 `totalGraceMs > 0` 驱动                                                                                                                                                             |
| S22 | 总超时后 `abort_grace` 实际时长 ≈ 1 tick：进 `abort_grace` 时 `clearAndArm` 以过去时 `deadlineAt` 重装 `total`，下一 tick 在 `abort_grace` 分支 `finish`                                                                                                | `src/core/state-machine.ts` `deadline_fired` 的 `abort_grace` 分支                                                                                                                                                                                                                                                                                                                                                                                                   | 真（RK-2）。`total_grace` 路径同构；"effects 逐项相等"断言改为"除总类 timer id 及其 dueAt 外逐项相等"；文档不得把 `abortGraceMs` 写成实测值                                                                                                                                                                                                                                                                               |
| S23 | workflow 子 run 与 goal 评估 run 恒带显式 `budgetOverride.totalMs`                                                                                                                                                                                      | `src/workflow/host.ts` `deps.spawner.spawn({ ..., budgetOverride: { totalMs: derived.totalMs, queueWaitMs } })`；`src/goal/hook.ts` `budgetOverride: { totalMs: settings.evalTimeoutMs }`                                                                                                                                                                                                                                                                            | D-10 下二者恒为硬顶 ⇒ RK-12(a)"子 run 宽限中、编排器已放弃"组合结构性消失；只需一条 full-stack 用例证明显式预算 run 不进宽限（V20）                                                                                                                                                                                                                                                                                       |

---

## 1. 已拍板事项（arch §11）

| #   | 问题                                      | 结论                                                             | 一句话理由                                                                  |
| --- | ----------------------------------------- | ---------------------------------------------------------------- | --------------------------------------------------------------------------- |
| Q1  | `/agent extend <run> <duration>` 人类命令 | **砍**；`ExtendSource = "tool"`（D-13）                          | 人类对模型说"给 reviewer 再加 10 分钟"即可；`"user"`/`"rpc"` 都是死枚举     |
| Q2  | `extend.notify` 默认值                    | `background`；`always` 仅调试（D-17）                            | 前台 `Agent` 调用到点时宿主仍被阻塞，通知是纯噪声；不复用 ackHold           |
| Q3  | 子 run 宽限通知经 fabric 路由给父 run     | 留 v2                                                            | workflow 子 run 因 D-10 本就不进宽限；嵌套 Agent 子 run 白得 `totalGraceMs` |
| Q4  | `maxTotalFactor` 默认                     | **2**                                                            | 30min 默认预算 run 最坏 60min；显式预算 run 恒为 1                          |
| Q5  | `totalGraceMs` 默认                       | **90s**，V1/V2 真机后复核                                        | 模型看到通知 → 思考 → 调工具在慢 provider 上 20–40s 是常态                  |
| Q6  | 显式 `timeout` run 是否享受宽限（BL-4）   | **不**——硬顶（D-10）                                             | `Agent` 工具描述 "always settles within this budget" 必须为真               |
| Q7  | `totalMs = 0`（BL-1）                     | **禁止**：`mergeBudget` 丢弃非法层回退下一层 + 加载 WARN（D-11） | 钳到最小值会让"想不限"的 run 秒死；回退默认与"键缺席"一致                   |
| Q8  | 时间单位（BL-4）                          | model/user-facing 秒；core/runtime 毫秒；RPC 毫秒不动（D-12）    | RPC 是跨进程稳定协议且非 model-facing                                       |

---

## 2. 开发包拆分（并行用）

拆分原则：**签名先行**。P0 由主会话串行落地所有跨包共享的类型/签名/存根（存根返回 `unsupported` 或 no-op，行为零变化），然后 A/B/C 三包并行，最后主会话串行接线。任意包只改自己 owned 的文件；要动冻结面就 message 主会话。

### 2.0 P0 · 冻结面（主会话，串行，2 个 commit）

| 文件                                           | 改动                                                                                                                                                                                                                                     |
| ---------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/core/types.ts`                            | 全部类型新增（§3.1）                                                                                                                                                                                                                     |
| `src/core/status.ts`（**新**）                 | `TERMINAL_STATUSES` / `isTerminalStatus` / `TerminalStatus`（D-15）                                                                                                                                                                      |
| `src/core/deadline.ts`                         | `DEFAULT_BUDGET` +3；`OVERTIME_PHASES`；5 个纯函数（§3.2）                                                                                                                                                                               |
| `src/service/ports.ts`                         | `Runner.extendDeadline?()` 签名                                                                                                                                                                                                          |
| `src/runtime/runner.ts`                        | **仅**：`TERMINAL_STATUSES` 改 import `core/status`；加 `extendDeadline()` 存根（返回 `{ok:false, reason:"unsupported"}`）；`dispatchers` 条目加 `budget`。不改任何行为                                                                  |
| `src/service/query-service.ts`                 | 接口 `extendTimeout()` 签名 + 存根实现（`runner.extendDeadline` 缺席 → `unsupported`）                                                                                                                                                   |
| `src/index.ts`                                 | `forwardQuery()` 增 `extendTimeout` 转发一行（否则 typecheck 失败）                                                                                                                                                                      |
| `src/config/settings.ts`                       | `mergeBudget` 丢弃非法 `totalMs` 层（D-11）；`loadSettingsFromFile` WARN；`ExtendSettings` + `fleetDeadlineWarnMs` + 默认值 + `parseExtendSettings` + `TIME_SETTING_MS_PATHS` +1（§3.7）                                                 |
| `src/config/setting-specs.ts`                  | `BUDGET_DESCRIPTIONS` +3 且 `totalMs` 改写；`seconds()` 增 `min?`；`BUDGET_SPECS` 白名单；`SETTING_SPECS` +3（§3.7）                                                                                                                     |
| `src/rpc/server.ts`                            | **仅注释**（S9）                                                                                                                                                                                                                         |
| `src/tools/agent-tool.ts`                      | `timeout_ms` → `timeout_s`（BL-4 / D-12，§3.8a）                                                                                                                                                                                         |
| `src/tools/workflow-tool.ts`                   | `timeout_ms` → `timeout_s`（D-12 一致性，§3.8a）                                                                                                                                                                                         |
| `tests/core/deadline-grace.test.ts`（新）      | 5 个纯函数的边界用例                                                                                                                                                                                                                     |
| `tests/config/extend-settings.test.ts`（新）   | 解析容错、秒↔毫秒、`maxTotalFactor` 非整数、`/agent settings` 键出现、`totalS: 0` → 默认 + WARN、`budget.totalS` spec `min === 1`                                                                                                        |
| `tests/config/agent-config.test.ts`            | `mergeBudget` 丢弃 `totalMs: 0` / 负 / NaN 层并回退下一层；返回值 `totalMs > 0` 恒成立（RK-11）                                                                                                                                          |
| `tests/config/settings-time-migration.test.ts` | 两处 fixture `totalS: 0` → `totalS: 1800`（S20；0 的行为迁到 `extend-settings.test.ts`）                                                                                                                                                 |
| `tests/tools/agent-tool.test.ts`               | `timeout_ms` → `timeout_s`（`timeout_s: 120` ⇒ `budgetOverride.totalMs === 120_000`）                                                                                                                                                    |
| 测试基线预置（S21）                            | `tests/core/core.test.ts` 顶部 `budget`、`tests/runtime/runtime.test.ts` 模块级 `budget`、`tests/integration/wiring.test.ts` `fastBudget`、`tests/service/deadline-cap.test.ts` `fastBudget` 各加 `totalGraceMs: 0`（P0 阶段行为零变化） |

**验收**：`npm run typecheck && npm test` 全绿；`git diff --stat` 不含 `state-machine.ts`；除 S20/S21/`agent-tool.test.ts` 列出的行外，现有 1800+ 用例零改动。

### 2.1 Pkg A · core 状态机 + runtime（最大包，关键路径）

- **owned**：`src/core/state-machine.ts`、`src/runtime/watchdog.ts`、`src/runtime/runner.ts`（填充存根 + `guardUntil` + `fireDeadline` 条件 cancel）、`tests/core/core.test.ts`、`tests/runtime/runtime.test.ts`、`tests/runtime/watchdog.test.ts`（新建）
- **冻结面**：`types.ts`/`status.ts`/`deadline.ts`/`ports.ts`（若发现类型缺口，message 主会话补 P0 修订 commit，不得自行改）
- **验收标准**：
  - `INPUT_KINDS` 14、`FLAT_MATRIX` 168，`deadline_extended` 列 12 格 oracle 按 arch §3.6 表填写（4 启动相位 + `abort_grace`/`reap`/`settled` 为 `illegal`；5 个 OVERTIME 相位为主路径）；旧 156 格（基线 `totalGraceMs: 0`）逐位不变
  - P6 改写、P11/P12（BL-3 口径）/P13/P14 新增、P3/P8/seeded 生成器含 `deadline_extended`（§3.11）
  - `deadline_fired{total_grace}` 与 `total` 原路径 effects **除总类 timer id 及其 dueAt 外逐项相等**（RK-2）
  - 宽限中 `total` ∉ `armedTimers`（RK-1 真 oracle）；宽限中残留 `total` 再 `deadline_fired{total}` → `illegal`（防御）
  - 宽限中 `stop_requested` 两种 timer 先后顺序 → 均 `aborted`（RK-12(c)）
  - `guardUntil`：延长后 prompt 不在旧时刻被判超时；`deadlineOf() === undefined` 防御分支不 `setTimer(0)`
  - `fireDeadline` 进宽限后不 cancel；`extendDeadline` 八种 `ExtendOutcome` 分支各一条（含 `not_started`）；并发双向顺序用例（arch §4.6）
- **自验**：`npx vitest run tests/core tests/runtime && npm run typecheck`

### 2.2 Pkg B · service + tools + 通知纯函数

- **owned**：`src/service/runtime-adapter.ts`、`src/service/query-service.ts`（替换存根 + `wait()` 默认）、`src/tools/extend-timeout-tool.ts`（新）、`src/delivery/deadline-notice.ts`（新）、`src/runtime/tool-scope.ts`（`RESERVED_TOOL_NAMES` +1）、`tests/tools/extend-timeout-tool.test.ts`（新）、`tests/tools/model-facing-strings.test.ts`、`tests/delivery/deadline-notice.test.ts`（新）、`tests/delivery/notifier.test.ts`（RK-11）、`tests/service/query-service-extend-timeout.test.ts`（新）、`tests/service/query-service-wait-default.test.ts`（RK-5）、`tests/service/runtime-adapter-extend.test.ts`（新）、`tests/runtime/tool-scope.test.ts`
- **冻结面**：`types.ts`/`deadline.ts`/`ports.ts`/`stack.ts`/`index.ts`
- **与 A 的解耦（RK-10，无 `it.skip`）**：query/tool 测试用实现 `ports.Runner` 的假 runner 返回预制 `ExtendOutcome`；adapter 的 `notify_deadline` 逻辑抽成导出的纯函数 `deadlineNoticeHandler(childRunIds, sink)`，测试**直接构造 `EffectEnvelope{ effect: { kind: "notify_deadline", notice } }` 喂一个只注册该 handler 的 `BasicEffectInterpreter`**，不需要 reducer 产 effect ⇒ B 独立全绿
- **验收标准**：
  - 八种拒绝文案各一条且均含"下一步该做什么"（arch §4.4，含 `not_started` 与显式预算 `no_headroom` 变体）；成功文案含 granted/requested/clamped/remaining/ceiling/rescued
  - schema `extend_s` `Type.Integer({ minimum: 1 })`；`execute` 把 `extend_s * 1000` 传给 `query.extendTimeout`；`run_id` description 含 "label"
  - `shouldDeliverDeadlineNotice` 真值表全覆盖（off / background+ack 前台 / background+ack 已转后台 / always / `extended` → display-only）
  - `formatDeadlineNotice` 两种 kind 快照：含短 id、剩余宽限、一条可直接抄的完整调用（`extend_s: <秒>`）、额度、"不作为也合法"、Task 行
  - adapter：`deadlineNoticeHandler` 子 run 丢弃 / 顶层透传；`extendDeadline` 透传
  - `wait()`：无 `overtime` 基准 `deadlineAt`（既有用例不变）；有 `overtime` 基准 `hardDeadlineAt`
  - `notifier.test.ts`：同一 runId 在不经 outbox 的情况下（模拟宽限通知路径）终态 `enqueue()` 仍成功、outbox 恰一条（锁死 D-4）
- **自验**：`npx vitest run tests/tools tests/service tests/delivery tests/runtime/tool-scope.test.ts && npm run typecheck`

### 2.3 Pkg C · UI + status

- **owned**：`src/ui/fleet-panel.ts`、`src/ui/fleet-widget.ts`、`src/commands/status.ts`、`tests/ui/fleet.test.ts`、`tests/ui/fleet-widget.test.ts`、`tests/commands/status.test.ts`
- **冻结面**：`types.ts`、`stack.ts`（`deadlineWarnMs` 由主会话接线时传入；C 只加 deps 字段并给默认）
- **验收标准**：
  - `FleetRow.remainingMs/inGrace/extensions` 三字段计算（含终态 undefined、`graceUntil` 优先）
  - `highlightOf`：宽限中 crit；`eff - now <= deadlineWarnMs` warn；无 `deadlineWarnMs` 或为 0 不产生新 warn
  - `widgetRowMain`：`⏳12m`/`⏳12m+1`/`⏳grace 58s` 渲染；丢弃阶梯 `deadline` 排在 `total` 之后；`inGrace` 时永不丢弃（宽度 60 仍可见，label 被截断）
  - `/agent status <run>` 增 `deadline`/`overtime` 两行；无 overtime 时不出现；`/agent budget` usage 文案删 "budget.totalS: 0 = no overall cap"（D-11）
- **自验**：`npx vitest run tests/ui tests/commands && npm run typecheck`

### 2.4 P-final · 接线（主会话，串行）

- **owned**：`src/stack.ts`、`src/index.ts`（工具注册）、`src/service/spawn-service.ts`（`applyBudgetPolicy` 调用 + `deps.extensionsEnabled` + BL-5 终态重建）、`tests/integration/timeout-grace-wiring.test.ts`（新）、`tests/integration/context-receipt-wiring.test.ts`（+1 用例）、`tests/integration/fleet-widget-lifecycle.test.ts`（+1）、`tests/service/deadline-cap.test.ts`（RK-11：`hardDeadlineAt` 断言 + 显式预算不进宽限）、`tests/service/spawn-service.test.ts`（终态重建保留两字段）、`tests/ui/settings-editor.test.ts`（新键可见）、`README.md`、`README.en.md`、`CHANGELOG.md`、`AGENTS.md`、`docs/dev/timeout-notify/arch.md`（状态行）

### 2.5 冲突预检

```
P0     : core/{types,status,deadline}.ts service/ports.ts runtime/runner.ts(存根) service/query-service.ts(存根) index.ts(1行)
         config/{settings,setting-specs}.ts rpc/server.ts(注释) tools/{agent-tool,workflow-tool}.ts
         tests/core/deadline-grace.test.ts tests/config/{extend-settings,agent-config,settings-time-migration}.test.ts tests/tools/agent-tool.test.ts
         基线预置: tests/core/core.test.ts tests/runtime/runtime.test.ts tests/integration/wiring.test.ts tests/service/deadline-cap.test.ts（各 1 行）
A      : core/state-machine.ts runtime/watchdog.ts runtime/runner.ts     tests/core/core.test.ts tests/runtime/{runtime,watchdog}.test.ts
B      : service/runtime-adapter.ts service/query-service.ts runtime/tool-scope.ts tools/extend-timeout-tool.ts delivery/deadline-notice.ts
         tests/tools/{extend-timeout-tool,model-facing-strings}.test.ts tests/delivery/{deadline-notice,notifier}.test.ts
         tests/service/{query-service-extend-timeout,query-service-wait-default,runtime-adapter-extend}.test.ts tests/runtime/tool-scope.test.ts
C      : ui/fleet-panel.ts ui/fleet-widget.ts commands/status.ts     tests/ui/{fleet,fleet-widget}.test.ts tests/commands/status.test.ts
final  : stack.ts index.ts service/spawn-service.ts tests/integration/* tests/service/{deadline-cap,spawn-service}.test.ts tests/ui/settings-editor.test.ts
         README.md README.en.md CHANGELOG.md AGENTS.md docs/dev/timeout-notify/arch.md
```

A/B/C 三包两两文件集合交集为空（`tests/runtime/` 内按文件切：A 拿 `runtime.test.ts`/`watchdog.test.ts`，B 拿 `tool-scope.test.ts`；`tests/service/` 内 B 拿三个 query/adapter 文件，final 拿 `deadline-cap`/`spawn-service`）。`runner.ts`/`query-service.ts` 在 P0 与 A/B 之间是**串行**关系（P0 先落，A/B 在其上修改）。P0 对 A/final 所有测试文件的"基线预置"只有一行 `totalGraceMs: 0`，在 A/final 开工前已落地。

---

## 3. 文件级改动清单

### 3.1 `src/core/types.ts`（P0）

| 位置                               | 改动                                                                                                                                                                                                 | 引用            |
| ---------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------- |
| `DeadlineBudget`                   | +`totalGraceMs: Millis`、`maxExtensions: number`、`maxTotalFactor: number`，各带 doc 注释；`totalMs` 注释改为"恒 > 0，非法值由 `mergeBudget` 丢弃"（D-11）                                           | arch §3.2       |
| `RunDeadlines`                     | +`readonly graceUntil?: Millis`、`readonly hardDeadlineAt?: Millis`；**改写** `deadlineAt` 注释为"只可向后移动、只经 `deadline_extended`、≤ `hardDeadlineAt`"；`graceUntil` 注释写明终态保留（BL-5） | arch §3.2、D-2  |
| `SpawnRequest.deadlineAt` 注释 ③   | 改写：B1"算一次、冻结"迁移到 `hardDeadlineAt = min(enqueuedAt + ceil(totalMs·factor), deadlineAt)`；`deadlineAt` 是初值 `min(enqueuedAt+totalMs, cap)`，后续延长永不越过 `hardDeadlineAt`            | arch D-2、R-8   |
| `SpawnRequest.budgetOverride` 注释 | 补一句：`totalMs` 有值 ⇒ 该 run 为显式预算 run，spawn-service 钳 `maxTotalFactor = 1`（硬顶，无宽限无延长）                                                                                          | D-10            |
| `RunDiagnostics`                   | +`hardDeadlineAt?: Millis`（镜像）、+`overtime?: { graces; grace?: { startedAt; until }; extensions; grantedMs; lastReason?; lastSource? }`                                                          | arch §3.2、BL-5 |
| `RunInput`                         | +`{ kind: "deadline_extended"; at; extendMs; source: ExtendSource; reason? }`                                                                                                                        | arch §3.2       |
| `RunEffect`                        | +`{ kind: "notify_deadline"; notice: DeadlineNotice }`                                                                                                                                               | arch §3.2       |
| 新导出                             | `export type ExtendSource = "tool"`（D-13）；`interface DeadlineNotice`（含 `suggestedExtendMs`）；`type ExtendOutcome`（reason 八种，含 `not_started`；与 `SetModelOutcome` 相邻）                  | arch §3.2、D-14 |

不改：`RunPhase`/`RUN_PHASES`（12）、`TimeoutReason`、`RunStatus`、`RunState`。

### 3.2 `src/core/status.ts`（P0，新）与 `src/core/deadline.ts`（P0）

| 文件 / 改动                                                              | 说明                                                                                           |
| ------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------- |
| `status.ts`：`TERMINAL_STATUSES` / `isTerminalStatus` / `TerminalStatus` | 只 import `types.js`；无其它依赖（D-15）                                                       |
| `deadline.ts`：`DEFAULT_BUDGET` +3                                       | `totalGraceMs: 90_000`、`maxExtensions: 3`、`maxTotalFactor: 2`                                |
| `OVERTIME_PHASES`                                                        | 五个可宽限/可延长相位（D-14）；`state-machine.ts` 复用同一常量，不许各写各的                   |
| `effectiveDeadlineAt(d)`                                                 | `d.graceUntil ?? d.deadlineAt`                                                                 |
| `applyBudgetPolicy(budget, { explicitTotal, extensionsEnabled })`        | D-10 / D-16；spawn-service 唯一调用点                                                          |
| `hardDeadlineAtFor(enqueuedAt, budget, capAt)`                           | arch §3.3 原文；`!(totalMs > 0) ⇒ undefined`（防御）                                           |
| `extendability(state, budget, now)`                                      | arch §3.3 原文；终态判定 import `status.js`；`not_started` 在 `stopping` 之后、`uncapped` 之前 |
| `graceWindow(state, budget, at)`                                         | arch §3.3 原文                                                                                 |
| `remainingFor`                                                           | **不动**                                                                                       |

### 3.3 `src/core/state-machine.ts`（Pkg A）

| 位置                                              | 改动                                                                                                                                                                                                                                                                                                                                                                                                                                       | 引用                  |
| ------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------- |
| import                                            | +`effectiveDeadlineAt`、`extendability`、`graceWindow`、`hardDeadlineAtFor`、`OVERTIME_PHASES`（`deadline.js`）；`isTerminalStatus`（`status.js`）；删本地 `terminal()`                                                                                                                                                                                                                                                                    | D-15                  |
| `INPUT_KINDS`                                     | 末尾追加 `"deadline_extended"`（13 → 14）                                                                                                                                                                                                                                                                                                                                                                                                  | arch §3.2             |
| 新函数 `activeTotalTimer(d)`                      | `graceUntil` → `{timer:"total_grace", dueAt}`；`deadlineAt` → `{timer:"total", dueAt}`；否则 undefined                                                                                                                                                                                                                                                                                                                                     | arch §3.4             |
| 新函数 `rearmTimers(state, budget)`               | 清全部 → 按 `state.phase` 重武装相位 timer（`dueAt` 上界 `min(phaseDue, activeTotal.dueAt)`）→ 武装 activeTotal；**不碰** `diag.phaseEnteredAt/lastEventAt`；注释写明 `arm_timer.dueAt` 无消费者、承重点是 timer id（RK-1）                                                                                                                                                                                                                | arch §3.4             |
| `clearAndArm()` 重构                              | 先写 `{ phase, diag: { ...diag, phase, phaseEnteredAt: at } }`，再委托 `rearmTimers`。156 格旧矩阵不变即为等价证明                                                                                                                                                                                                                                                                                                                         | arch §3.4             |
| `enqueued` 分支                                   | `deadlineAt` 计算一字不改；新增 `hardDeadlineAt = hardDeadlineAtFor(...)`，写入 `deadlines` **与 `diag.hardDeadlineAt` 镜像**（缺席不写）；effects 不变                                                                                                                                                                                                                                                                                    | arch §3.5(a)、BL-5    |
| `deadline_fired` 分支                             | ① 在 `armedTimers.includes` 检查之后插入防御：`timer === "total" && graceUntil !== undefined → illegal`（RK-1）；② 在 `abort_grace` 分支之后、通用杀路径之前插入：`timer === "total"` → `graceWindow()` 有值则置 `graceUntil`、`overtime.graces++`、`overtime.grace = { startedAt, until }`，`rearmTimers`，追加 `notify_deadline{grace}`；无值落通用路径。`total_grace` 到点不加任何特判                                                  | arch §3.5(b)、§3.6    |
| 新分支 `deadline_extended`                        | 放在 `deadline_fired` 之前；逻辑按 arch §3.5(c)：`extendability` 不 ok → `illegal`（含 `not_started`）；`base = max(at, prev)`；`next = min(base + max(0, extendMs), hard)`；净增益 0 → `illegal`；更新 `deadlines.deadlineAt`、`diag.deadlineAt` 镜像、`overtime`（`extensions++`、`grantedMs`、`lastReason.slice(0,200)`、`lastSource`、**删 `grace`**）、**删 `deadlines.graceUntil`**；`rearmTimers`；追加 `notify_deadline{extended}` | arch §3.5(c) 三条禁令 |
| 新函数 `buildGraceNotice` / `buildExtendedNotice` | 纯数据组装；`suggestedExtendMs = min(budget.totalMs, hardDeadlineAt - max(at, deadlineAt))`；`label/agentType/taskPreview(折叠空白、120 字符)` 来自 `diag`                                                                                                                                                                                                                                                                                 | arch §5.3、S15        |
| `envelope()`                                      | 不改（`notify_deadline` 默认 best_effort）                                                                                                                                                                                                                                                                                                                                                                                                 | arch §3.5             |
| 注释改写                                          | `enqueued` 分支里 "CC4/FF2: the core B1 invariant" 注释改为"B1 现在保护 `hardDeadlineAt`；`deadlineAt` 单调不减且 ≤ H"                                                                                                                                                                                                                                                                                                                     | R-8                   |

**行为边界说明**（写进代码注释）：宽限中收到 `stop_requested` → `enter(abort_grace)` → `rearmTimers` 仍同时武装 `abort_grace` 与 `total_grace`，谁先到谁杀，终态恒 `aborted`——与今天 `total` 在 `abort_grace` 中并存的行为同构；`abort_grace` 在总超时路径上实际只活 ≈1 tick（S22）。

### 3.4 `src/runtime/watchdog.ts`（Pkg A）

- `timerReason` +`total_grace: "total"`
- `tick()`：`const due = timer === "total" ? deadlineAt : timer === "total_grace" ? graceUntil : dueAtFor(...)`
- 新建 `tests/runtime/watchdog.test.ts`（此前无独立 watchdog 测试）：arch §9.3 四条——oracle 为"宽限中 `total` ∉ armedTimers"（RK-1），配合 reducer 防御用例

### 3.5 `src/runtime/runner.ts`（P0 存根 → Pkg A 填充）

| 改动                                                                                         | 说明                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| -------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| import                                                                                       | +`effectiveDeadlineAt`、`extendability`（`deadline.js`）；`isTerminalStatus`（`status.js`，删本地 `TERMINAL_STATUSES`）；`ExtendOutcome`、`ExtendSource` 类型                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `dispatchers`                                                                                | `Map<string, { gen; fn; budget: DeadlineBudget }>`（P0），`run()` 里 `set` 时带 budget                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `extendDeadline(runId, extendMs, opts: { source; reason? }): ExtendOutcome`（**同步**，D-9） | ① `states.get` 缺 → `unknown_run`；② `dispatchers.get` 缺/代次不符 → `already_terminal`（run 已 finally）；③ `verdict = extendability(state, budget, now)` 不 ok → 映射同名 reason（含 `not_started`）；④ 记 `prevDeadline`、`wasInGrace = graceUntil !== undefined`；⑤ `dispatchExternal(deadline_extended)`；⑥ 回读 `states.get`：`deadlineAt` 未变 → `no_headroom`；⑦ 组装 ok 结果：`grantedMs = deadlineAt - max(now, prev)`、`clamped = granted < requested`、`extensionsRemaining = budget.maxExtensions - overtime.extensions`、`rescuedFromGrace = wasInGrace`。方法体内**零 `await`** |
| `guardUntil<T>(p, deadlineOf, cancel, label)` 新私有方法                                     | timer 到点时重读 `deadlineOf()`：`undefined` → 不重装（**防御分支**，注释引用 D-11）；`due > now` → 按 `due - now` 重装；`due <= now` → resolve timeout。首次武装同逻辑。`finish()` 清理同 `guard`                                                                                                                                                                                                                                                                                                                                                                                             |
| prompt 处                                                                                    | 删除 `promptBudget = remainingFor(budget.totalMs, …)`；改 `await this.guardUntil(handle.prompt(req.prompt), () => effectiveDeadlineAt(state.deadlines), cancel, "prompt")`。`queue/create/bind` 三处 `guard` **不动**                                                                                                                                                                                                                                                                                                                                                                          |
| `fireDeadline()`                                                                             | **必须改**：`dispatchExternal` 后回读 `this.states.get(runId)`，仅当 `state.status === "stopping" \|\| isTerminalStatus(state.status)` 才 `cancel.cancel("timeout")`。否则宽限刚进入就被 cancel（评审确认为关键点，配专门用例 + P14）                                                                                                                                                                                                                                                                                                                                                          |

### 3.6 service 层（P0 签名 → Pkg B 实现 → P-final spawn-service）

| 文件                                                   | 改动                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/service/ports.ts`（P0）                           | `Runner.extendDeadline?(runId, extendMs, opts: { source: ExtendSource; reason?: string }): ExtendOutcome`，注释：同步、老实现缺席即 `unsupported`                                                                                                                                                                                                                                                                                                                                                          |
| `src/service/query-service.ts`（P0 签名+存根；B 实现） | 接口 `extendTimeout(runId, extendMs, opts): ExtendOutcome`（**同步**，注释引用 arch §4.6）；实现：`registry.get` 缺 → `unknown_run`；`runner.extendDeadline` 缺 → `unsupported`；否则直返 runner 结果。`wait()`：`remaining` 基准 = `snapshot.diag.overtime !== undefined ? (hardDeadlineAt ?? deadlineAt) : deadlineAt`（RK-5），注释写明"只有真的进过宽限/被延长的 run 才用天花板"                                                                                                                       |
| `src/service/runtime-adapter.ts`（B）                  | `RuntimeAdapterDeps` +`onDeadlineNotice?: (notice: DeadlineNotice) => void`；**导出**纯函数 `deadlineNoticeHandler(childRunIds: ReadonlySet<string>, sink?: (n) => void): (e: RunEffect) => void`（`e.kind !== "notify_deadline"` 直接 return；`childRunIds.has(runId)` 丢弃；否则 `sink?.(notice)`）；`BasicEffectInterpreter` handlers +`notify_deadline: deadlineNoticeHandler(childRunIds, deps.onDeadlineNotice)`；返回的 `Runner` +`extendDeadline(...) { return runtime.extendDeadline(...); }`     |
| `src/service/spawn-service.ts`（**P-final**）          | ① `SpawnServiceDeps` +`extensionsEnabled?: boolean`（默认 true）；② `spawn()` 内 `const budget = applyBudgetPolicy(mergeBudget(deps.budget, config.budgetOverride, req.budgetOverride), { explicitTotal: req.budgetOverride?.totalMs !== undefined, extensionsEnabled: deps.extensionsEnabled ?? true })`（D-10/D-16）；③ 终态重建 `deadlines` 补 `...(diag.hardDeadlineAt !== undefined ? { hardDeadlineAt } : {})`、`...(diag.overtime?.grace ? { graceUntil: diag.overtime.grace.until } : {})`（BL-5） |
| `src/rpc/server.ts`（P0，**仅注释**）                  | `clampBudget` 内 `Math.max(1, bounded)` 的注释改写（S9）；**不**加 `DEFAULT_CAPS`；**不**改 `tests/rpc/rpc.test.ts`                                                                                                                                                                                                                                                                                                                                                                                        |

### 3.7 config（P0）

| 文件                          | 改动                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ----------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/config/settings.ts`      | ① `mergeBudget`：逐层丢弃非法 `totalMs`（非有限 / ≤ 0），回退下一层（D-11，arch §3.3 代码）；② `loadSettingsFromFile`：`loadSettings` 之前读原始 `budget.totalS`，`≤ 0` 则 `console.warn("[pi-subagent] budget.totalS must be > 0 (got 0); using the default 1800s")` 一次；③ `export interface ExtendSettings { enabled: boolean; notify: "background" \| "always" \| "off" }`；`AgentSettings` +`extend`、`fleetDeadlineWarnMs`；`DEFAULT_SETTINGS` +`extend: { enabled: true, notify: "background" }`、`fleetDeadlineWarnMs: 60_000`；`TIME_SETTING_MS_PATHS` +`"fleetDeadlineWarnMs"`；`loadSettings` +`extend: parseExtendSettings(value.extend)`、`fleetDeadlineWarnMs`（与 `fleetTerminalLingerMs` 同款校验）；`export function parseExtendSettings(input: unknown): ExtendSettings`（never throws）                                        |
| `src/config/setting-specs.ts` | `BUDGET_DESCRIPTIONS`：`totalMs` 改 `"Overall run cap (must be > 0; 0 falls back to the default)"`，+3 条（arch §7.1 文案）；`seconds()` 增 `min?: number` 选项（默认 0）；`BUDGET_SPECS` 生成器改四分支：`startupRetries`/`maxExtensions` → `{...count(path, 0, desc), live: true}`；`maxTotalFactor` → `{ kind: "number", path, min: 1, live: true, description }`（允许 1.5）；`totalMs` → `seconds(path, { live: true, min: 1, description })`；其余 `seconds()`。`SETTING_SPECS` 在 `"compact.enabled"` 之后、`"cacheTtl.mode"` 之前插入 `"extend.enabled": bool(...)`、`"extend.notify": choice(..., ["background","always","off"], "… always = debug only")`、`fleetDeadlineWarnS: seconds("fleetDeadlineWarnMs", { hint: "0 disables the deadline warn tier", description: "Fleet row turns warn when remaining time drops below this" })` |

`/agent settings` 验收键名清单见 arch §7.3。

### 3.8 tools

#### 3.8a `timeout_ms` → `timeout_s`（P0，BL-4 / D-12）

| 文件                             | 改动                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| -------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `src/tools/agent-tool.ts`        | schema 字段 `timeout_ms: Type.Optional(Type.Number({...}))` → `timeout_s: Type.Optional(Type.Integer({ minimum: 1, description: "Optional total wall-clock budget for this run in seconds (overrides the default 30min). An explicit timeout is a hard cap: the run always settles within it — no grace window, no extension. Omit it to use the default budget, which gets a grace window at expiry and can be extended with extend_subagent_timeout." }))`；`execute` 内 `budgetOverride: { totalMs: params.timeout_s * 1000 }`（`typeof params.timeout_s === "number"` 守卫保留） |
| `src/tools/workflow-tool.ts`     | schema `timeout_ms` → `timeout_s: Type.Optional(Type.Integer({ minimum: 1, description: "Total wall-clock budget for the whole workflow run, in seconds (overrides the default)." }))`；文件头注释 "bounded by its own timeout_ms" → `timeout_s`；`promptSnippet` 参数列表 `timeout_ms?` → `timeout_s?`；`renderCall` `formatDuration(args.timeout_s * 1000)`；`execute` `mergeBudget(deps.defaultBudget, params.timeout_s === undefined ? undefined : params.timeout_s * 1000)`（`mergeBudget` 内部仍收毫秒，签名不变）                                                             |
| `tests/tools/agent-tool.test.ts` | describe/it 名与参数改 `timeout_s`；断言 `budgetOverride.totalMs === 120_000`（输入 `timeout_s: 120`）；新增：`timeout_s` 缺席 ⇒ 请求无 `budgetOverride`                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `README.md` / `README.en.md`     | `Agent` 工具参数行 `timeout_ms` → `timeout_s`，并补半句"（秒；显式 timeout 为硬顶，不宽限不延长）"/"(seconds; an explicit timeout is a hard cap)"（P-final 一起提）                                                                                                                                                                                                                                                                                                                                                                                                                  |

**不改**：`docs/dev/auto-background/plan.md`、`docs/dev/timeout-notify/explore.md`（历史记录）；`result-tool.ts`/`bash-job-tool.ts` 的 `wait_ms`（§7）；RPC 协议的 `budgetOverride.*Ms`（D-12 例外：跨进程稳定协议、非 model-facing）。

#### 3.8b 新工具（Pkg B）

| 文件                                       | 改动                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/tools/extend-timeout-tool.ts`（新）   | `ExtendTimeoutParams`（arch §4.1 原文 schema，`extend_s: Type.Integer({ minimum: 1 })`）；`createExtendTimeoutTool(deps: { query: Pick<QueryService,"extendTimeout"\|"get">; resolveRun?; now?: () => number })`；`name: "extend_subagent_timeout"`、`label: "Extend Subagent Timeout"`、`description`/`promptSnippet`（不含 `§`/"architecture"）；`renderCall` 同 `abort-tool.ts`：标题 + 灰色 `+10m · <reason 预览 80 字符>`；`execute`：`resolveRun` → `query.extendTimeout(runId, params.extend_s * 1000, { source: "tool", reason })`（同步一行）→ ok 走 arch §4.5 文案；`!ok` 按 arch §4.4 矩阵 `throw new Error(文案)`，`limit_reached`/`no_headroom`/`not_started` 文案里的剩余时间与 status 用 `query.get(runId)` 算；`no_headroom` 在 `hardDeadlineAt === deadlineAt && extensionsUsed === 0` 时走"显式 timeout 硬顶"变体 |
| `src/runtime/tool-scope.ts`                | `RESERVED_TOOL_NAMES` +`"extend_subagent_timeout"`（注释沿 `set_model` 先例）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `tests/tools/model-facing-strings.test.ts` | `tools()` +`createExtendTimeoutTool({ query: {} as never })`；label 断言 +1                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |

### 3.9 delivery（Pkg B 纯函数；P-final 接线）

| 文件                                    | 改动                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| --------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/delivery/deadline-notice.ts`（新） | `export const TIMEOUT_NOTICE_TYPE = "subagent:timeout"`；`formatDeadlineNotice(notice, ctx: { now: Millis; snapshot?: RunSnapshot }): string`——`grace` 按 arch §5.3 七行模板（`extend_s: Math.max(1, Math.round(suggestedExtendMs / 1000))`；`Now:` 行从 `snapshot.diag` 取，缺快照则省略），`extended` 按 arch §5.4 单行；`shouldDeliverDeadlineNotice(notice, ctx: { policy: ExtendSettings["notify"]; expectsAck(runId): boolean; autoBackgrounded(runId): boolean }): boolean`——arch §5.5 规则 2/3（`always` 忽略规则 2）；`deliveryOptionsFor(notice): { triggerTurn: boolean }`——`grace` → true，`extended` → false；`overtimeTail(diag): string`——`diag.overtime` 存在且 `graces > 0 \|\| extensions > 0` 时返回 ` (finished in overtime; N extension(s) used)`，否则 `""`                                                                                                                                                                                                                                                       |
| `src/stack.ts`（P-final）               | ① `createSpawnService({ ..., extensionsEnabled: settings.extend.enabled })`（D-16；**不再**派生 `effectiveBudget` 副本）；② `const sendDeadlineNotice = (notice) => { if (!shouldDeliverDeadlineNotice(notice, { policy: settings.extend.notify, expectsAck: (id) => spawnRef.current?.expectsAck(id) === true, autoBackgrounded: (id) => query.get(id)?.diag.autoBackgroundedAt !== undefined })) return; pi.sendMessage({ customType: TIMEOUT_NOTICE_TYPE, content: formatDeadlineNotice(notice, { now: systemClock.now(), snapshot: query.get(notice.runId) }), display: true, details: notice }, deliveryOptionsFor(notice)); }`（放在 `sendFormatted` 旁）；③ `createRuntimeRunnerAdapter({ …, onDeadlineNotice: sendDeadlineNotice })`；④ `sendFormatted` 单条分支 `completed` 文案追加 `overtimeTail(snapshot?.diag)`；⑤ `FleetWidgetController({ …, deadlineWarnMs: settings.fleetDeadlineWarnMs })`；⑥ `defaultWaitMs` 注释补一句"宽限/延长可能让 run 活过此值；有 overtime 的 run 由 `wait()` 动态基准接管"（数值不变，RK-5） |
| `src/index.ts`（P-final）               | `createAbortTool` 注册行之后：`if (settings.extend.enabled) pi.registerTool(createExtendTimeoutTool({ query: forwardQuery(holder), resolveRun: forwardResolveRun(holder), now: () => systemClock.now() }));`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |

不改：`notifier.ts`/`engine.ts`/`coalescer.ts`/`context-receipt.ts`（D-4：宽限通知不进 outbox；D-17：不复用 ackHold）。

### 3.10 UI（Pkg C）

| 文件                     | 改动                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/ui/fleet-panel.ts`  | `FleetRow` +`remainingMs: Millis \| undefined`、`inGrace: boolean`、`extensions: number`（arch §6.1）；`FleetViewOptions` +`deadlineWarnMs?: Millis`；`toRow()` 三行计算（`eff = graceUntil ?? deadlineAt`）；`highlightOf()` 签名 opts +`deadlineWarnMs?`，在 `stopping` 之后插入 `graceUntil !== undefined → "crit"`，在 idle-warn 之后追加 `deadlineWarnMs !== undefined && deadlineWarnMs > 0 && eff !== undefined && eff - now <= deadlineWarnMs → "warn"`（arch §6.2）；文件头注释的高亮规则列表同步 |
| `src/ui/fleet-widget.ts` | `widgetRowMain` fields 在 `phase` 后插入 `deadline`（`row.remainingMs !== undefined` 时），新增 `deadlineField(row)`；丢弃阶梯末尾 `if (!fits() && !row.inGrace) drop("deadline")`（arch §6.3）；`FleetWidgetDeps` +`deadlineWarnMs?: Millis`，`renderFrame()` 透传给 `buildFleetViewModel`                                                                                                                                                                                                                |
| `src/commands/status.ts` | `renderRunDetail()` 在 `Usage` 行之前插入 `deadline` 行（`deadlineAt` 相对/`graceUntil` 剩余/`hardDeadlineAt` 相对）与 `overtime` 行（`graces/extensions/granted/reason`），均仅在对应字段存在时输出（arch §6.4）；`/agent budget` usage 文案删去 "(budget.totalS: 0 = no overall cap)"（D-11）                                                                                                                                                                                                            |

### 3.11 tests 同步清单（按包）

| 包    | 文件                                                       | 要点                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ----- | ---------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P0    | `tests/core/deadline-grace.test.ts`（新）                  | `hardDeadlineAtFor`：factor<1 按 1；cap 更紧取 cap；`totalMs ≤ 0` 防御 undefined。`applyBudgetPolicy` 四组合。`extendability` 七种 reason（含 `not_started` 四相位）。`graceWindow`：`totalGraceMs=0`/`maxExtensions=0`/触顶(`at===H`)/`maxTotalFactor=1`/正常。`effectiveDeadlineAt` 优先级。`isTerminalStatus` 四真四假                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| P0    | `tests/config/extend-settings.test.ts`（新）               | `parseExtendSettings` 容错；`loadSettings` 中 `budget.totalGraceS: 30` → `totalGraceMs: 30000`；`budget.maxTotalFactor: 1.5` 保留小数；`budget.totalS: 0` → `totalMs === 1_800_000`；`loadSettingsFromFile` 对 `totalS: 0` 文件 `warn` 恰一次且不改写文件；`SETTING_SPECS` 含三新键、`budget.totalS.min === 1`、`maxExtensions`/`maxTotalFactor` 非 time spec                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| P0    | `tests/config/agent-config.test.ts`                        | `mergeBudget({ totalMs: 0 })` → 默认；`mergeBudget({ totalMs: 3600_000 }, { totalMs: -1 })` → 3600_000（回退上一层）；`NaN`/`Infinity` 同；`mergeBudget(...).totalMs > 0` 恒成立                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| P0    | `tests/config/settings-time-migration.test.ts`             | 两处 fixture `totalS: 0` → `1800`；期望值同步                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| P0    | `tests/tools/agent-tool.test.ts`                           | §3.8a                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| P0    | 基线预置                                                   | `tests/core/core.test.ts`、`tests/runtime/runtime.test.ts`、`tests/integration/wiring.test.ts`、`tests/service/deadline-cap.test.ts` 的基线 budget 各加 `totalGraceMs: 0`（S21）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| A     | `tests/core/core.test.ts`                                  | ① 顶部 `input(kind)` 助手 +`deadline_extended` 分支（当前末尾 fallback 返回 `effect_failed` 形状，新 kind 会走错）；② `buildInput()` +case；③ `MATRIX` 12 行各 +`deadline_extended` 列（arch §3.6：4 启动相位 + `abort_grace`/`reap`/`settled` 为 illegal；5 个 OVERTIME 相位 effects `["clear_timer","clear_timer","arm_timer","arm_timer","notify_deadline"]`）；④ `toHaveLength(13)`→14、156→168、`Object.keys(MATRIX[phase])` 14；⑤ 三处 deadline 不变断言改写：`invariants` 套件、P6、seeded "budget and deadline are immutable" → 断言 `deadlineAt` 单调不减 ∧ `≤ hardDeadlineAt` ∧ `hardDeadlineAt` 恒定；⑥ `randomInput`/`randomInputComprehensive` +`deadline_extended` 分支（`extendMs` 1s–1h、`source: "tool"`）；⑦ 新增 P11/P12（`grace ≤ N+1`、`extended ≤ N`、总 `≤ 2N+1`）/P13/P14 进 `it.each` 表；⑧ 新 `describe("timeout grace")`：`totalGraceMs: 60_000` 驱动的 5×6 判定矩阵、`total_grace` 与 `total` 原路径 effects 归一化后相等、宽限中 `tool_start/tool_end/phase_entered` 后 `armedTimers` 含 `total_grace` 且不含 `total`、残留 `total` 防御 illegal、宽限中 `stop_requested` 双顺序均 `aborted`、延长不改 `phaseEnteredAt/lastEventAt`（P13 定向版）、N=3 最坏序列恰好 3 grace + 3 extended |
| A     | `tests/runtime/runtime.test.ts`                            | `guardUntil`：假时钟 + 永不 resolve 的 prompt，t=totalMs 前 `extendDeadline`，推进到旧 deadline 断言 run 仍 running，推进到新 deadline 断言 timed_out；`deadlineOf() === undefined` 防御分支不 `setTimer(0)`（直接构造 deadlines）；`extendDeadline` 八分支（含 `not_started`：在 `queue_wait` 中调用）；宽限中延长 `rescuedFromGrace: true` 且 `grantedMs` 以 now 为基准；`clamped`；`fireDeadline` 进宽限后 **不** cancel（`prompt` 未被 abort）；双向竞态顺序；`clock.pendingTimers` 在宽限/延长路径不新增常驻 interval（V14）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| A     | `tests/runtime/watchdog.test.ts`（新）                     | arch §9.3 四条                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| B     | `tests/tools/extend-timeout-tool.test.ts`（新）            | 假 `query.extendTimeout` 返回九种 `ExtendOutcome`（8 拒绝 + ok）逐条断言文案关键词；`extend_s: 600` ⇒ `extendTimeout(..., 600_000, ...)`；`resolveRun` 前缀/label/失败 throw；`renderCall` 含 `+10m`；schema `minimum: 1`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| B     | `tests/delivery/deadline-notice.test.ts`（新）             | 两种 kind 快照（`extend_s: 600`）；`shouldDeliverDeadlineNotice` 真值表；`deliveryOptionsFor`；`overtimeTail` 三态                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| B     | `tests/delivery/notifier.test.ts`                          | 回归：对某 runId 不调用 `enqueue` 的任何"宽限期"操作后，终态 `enqueue()` 成功且 outbox 恰一条（锁死 D-4 的"不占 key"）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| B     | `tests/service/query-service-extend-timeout.test.ts`（新） | `unknown_run`/`unsupported`/透传                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| B     | `tests/service/query-service-wait-default.test.ts`         | +2：快照无 `overtime` → 基准 `deadlineAt`（既有用例不变）；快照有 `overtime` 且 `hardDeadlineAt` → 基准天花板，延长后不提前 `wait_timeout`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| B     | `tests/service/runtime-adapter-extend.test.ts`（新）       | `deadlineNoticeHandler`：构造 `EffectEnvelope` 喂 `BasicEffectInterpreter`——子 run 丢弃 / 顶层透传 / 非 `notify_deadline` 忽略（RK-10，无 skip）；`extendDeadline` 透传到假 runtime                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| B     | `tests/runtime/tool-scope.test.ts`                         | RESERVED +1                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| C     | `tests/ui/fleet.test.ts`                                   | `toRow` 三字段；`highlightOf` 新两条边界（`eff - now === warnMs` 是 warn，+1ms 不是；`deadlineWarnMs: 0` 关闭）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| C     | `tests/ui/fleet-widget.test.ts`                            | 三种渲染；丢弃阶梯顺序（复用 "drops fields by priority" 的宽度递减手法）；`inGrace` 宽度 60                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| C     | `tests/commands/status.test.ts`                            | 两行出现/不出现；budget usage 文案不含 "0 = no overall cap"                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| final | `tests/integration/timeout-grace-wiring.test.ts`（新）     | 用 `wiring.test.ts` 的 `buildStack` 手法 + 假 `sendMessage` 收集器，默认预算 run（**不带** `budgetOverride.totalMs`，budget 由 deps 注入 `totalMs 2000 / totalGraceMs 1000 / maxExtensions 1 / maxTotalFactor 2`）：① t=2000 收到 `subagent:timeout` + `triggerTurn:true` 且 run 仍 running → `queryService.extendTimeout(+1500)` → 活过 3000 → 完成 → outbox `sent.length === 1` 且文案含 "finished in overtime"；② 不延长 → t=3000 `timed_out`、`timeoutReason:"total"`、`sent.length === 1`；③ `extensionsEnabled: false` + agent-type `budgetOverride.maxExtensions: 3` → t=2000 直接 `abort_grace`，无 `subagent:timeout`（RK-8）；④ 显式 `budgetOverride: { totalMs: 2000 }` → t=2000 直接 `abort_grace`（D-10）；⑤ `SingleSlotPool`（limit=1）：A 宽限中，B 排队 `queueWaitMs` 到点 → B `failed/queue_timeout`，A 仍 running 至 `graceUntil`（RK-4）；⑥ 两次 `buildStack` 共用假 pi：旧 stack 的 run 宽限中重建 → 旧 watchdog 仍在 `graceUntil` 杀掉、`subagent:timeout` 恰一条、新 stack `extendTimeout` 对旧 run 返回 `unknown_run`（RK-12(b)）                                                                                                                                                              |
| final | `tests/integration/context-receipt-wiring.test.ts`         | +1：`subagent:timeout` 消息不触发 `noteEntered`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| final | `tests/integration/fleet-widget-lifecycle.test.ts`         | `/reload` 后 `sendDeadlineNotice` 闭包随旧 stack 失效（R-13），沿现有用例形态加一条                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| final | `tests/service/deadline-cap.test.ts`                       | +3：显式 `budgetOverride.totalMs` ⇒ `hardDeadlineAt === deadlineAt`；带 `deadlineAt` cap ⇒ `hardDeadlineAt === cap`；显式预算 run + `totalGraceMs: 60_000` 到点直接 `timed_out`（无宽限）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| final | `tests/service/spawn-service.test.ts`                      | +1：终态重建后的 `registry.get(runId).deadlines` 含 `hardDeadlineAt` 与（宽限中完成时）`graceUntil`（BL-5）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| final | `tests/ui/settings-editor.test.ts`                         | 新键出现且描述非空；`budget.totalS` 输入 `0` 被拒（min 1）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |

---

## 4. 实施顺序

```
P0 冻结面（2 commits）──┬── Pkg A（core 状态机 + runtime）──┐
（主会话）                ├── Pkg B（service + tools + 通知）──┼── P-final 接线 + spawn-service + 集成测试 + 文档 ── 真机验证 V1–V24
                          └── Pkg C（UI + status）────────────┘
```

| 步  | 内容               | 依赖 | 备注                                                                                                                                                            |
| --- | ------------------ | ---- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 0   | P0                 | —    | 必须先做：类型是所有人的依赖；存根保证 A/B/C 各自 typecheck 可独立通过；基线预置 `totalGraceMs: 0` 保证 A 合入时既有测试不因默认宽限而变红                      |
| 1   | A ∥ B ∥ C          | P0   | A 是关键路径（状态机 + 矩阵 + 属性测试 + runner），预计耗时约为 B 的 1.5–2 倍、C 的 2–3 倍；B/C 完成后主会话可先 review 它们。B 无 `it.skip`，独立全绿（RK-10） |
| 2   | 合入顺序 A → B → C | 1    | 无代码耦合，顺序仅为 review 便利                                                                                                                                |
| 3   | P-final            | 2    | stack/index/spawn-service 接线 + 集成测试 + README/CHANGELOG/AGENTS.md/arch 状态行                                                                              |
| 4   | 真机验证           | 3    | §5 的 V 表逐条记录到本文 §5 结果列（参照 compact-hint/agent-label 两份 plan 的做法）                                                                            |

每一步结束都要 `npm run format:check && npm run typecheck && npm test && npm run build` 全绿。

---

## 5. 验收矩阵

验证方法：**U** = 单测，**I** = 集成（vitest），**M** = 真机 pi 会话。真机步骤：`/agent settings set budget.totalGraceS 30`、`set budget.totalS 20`（**默认预算路径**：spawn 时**不带** `timeout_s`——带了就是硬顶，见 V23），让 subagent 跑 `sleep 60` 之类的长 bash。

| #   | 场景                                       | 期望                                                                                                                                                                                                                                                    | 方法                                   | 结果 |
| --- | ------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------- | ---- |
| V1  | 到点进宽限并通知                           | t=deadline：run 仍 `running`/相位不变；`armedTimers` 含 `total_grace` 不含 `total`；主会话收到 `subagent:timeout` + `triggerTurn:true`，文案含短 id、剩余宽限、完整可抄调用（`extend_s: <秒>`）、额度；widget 主行转红显示 `⏳grace Ns`                 | U(A 矩阵) + I + M                      |      |
| V2  | 宽限内延长续跑                             | 工具返回 granted/remaining/ceiling 且 `rescuedFromGrace`；`graceUntil` 清除、`total` 重装；prompt guard **不**在旧时刻超时；widget 显示 `⏳Xm+1`                                                                                                        | U(A runner) + I + M                    |      |
| V3  | 宽限耗尽照杀                               | t=graceUntil：进 `abort_grace` → `timed_out`、`timeoutReason:"total"`；effects 序列与今天 `total` 到点除总类 timer id 外逐项相等；`abort_grace` 实际 ≈1 tick（S22）；终态通知恰一条（timed_out 不加 overtime 尾巴）；`get_subagent_result` 拿到部分输出 | U(A 等价用例) + I + M                  |      |
| V4  | 次数上限拒绝 + 通知上界                    | 第 `maxExtensions+1` 次调用 throw `limit_reached` 文案（含 "in Ns" 与两条出路）；此后到点不再进宽限，直接 `timed_out`；`notify_deadline`：`grace ≤ N+1`、`extended ≤ N`、总 `≤ 2N+1`（P12）；N=3 最坏序列实测 3+3                                       | U(B tool + A P12/定向) + M             |      |
| V5  | 天花板夹紧                                 | `maxTotalFactor=1.2` 下请求超大 `extend_s` → `clamped:true`、`grantedMs < requestedMs`，文案如实；再次请求 → `no_headroom`                                                                                                                              | U(A/B) + M                             |      |
| V6  | 宽限内自然完成                             | `completed`；终态通知尾巴 ` (finished in overtime; N extension(s) used)`；outbox 恰一条（P10/D-4）；**`registry.get(runId).deadlines.graceUntil` 与 `hardDeadlineAt` 留在终态快照**（BL-5，经 spawn-service 重建路径）                                  | I + M                                  |      |
| V7  | 宽限不掩盖真挂死                           | 宽限中 `idle`/`tool` 子阶段 timer 照常触发并杀，`timeoutReason` 为 `idle`/`tool:*`                                                                                                                                                                      | U(A watchdog) + M                      |      |
| V8  | 宽限中用户 abort                           | 立即 `abort_grace`，`min(abortGraceMs, graceUntil-now)` 内 `aborted`                                                                                                                                                                                    | U(A) + M                               |      |
| V9  | 前台 caller-ack 抑制                       | `notify:"background"` + `expectAck` 前台 run → 不发；转后台（`autoBackgroundedAt` 置位）后到点 → 发；`notify:"off"` 全不发但宽限仍生效；`notify:"always"` 前台也发（调试口径）                                                                          | U(B 真值表) + M                        |      |
| V10 | 嵌套 Agent 子 run（CC2）                   | 父 agent 用不带 `timeout_s` 的 Agent 工具 spawn 子 run；顶层无 `subagent:timeout`；子 run 白得宽限后 `timed_out`                                                                                                                                        | U(B adapter) + M                       |      |
| V11 | `extend.enabled=false`                     | `/reload` 后工具消失；spawn-service 钳 `maxExtensions:0` → 到点即进 `abort_grace`；**agent-type `budgetOverride.maxExtensions: 3` 程序注入时仍无宽限**（RK-8/D-16）                                                                                     | I + M                                  |      |
| V12 | 窄终端 + 宽限                              | 宽度 60 主行仍含 `⏳grace Ns`，label 被截断而非 deadline 被丢                                                                                                                                                                                           | U(C) + M                               |      |
| V13 | `totalS: 0` 禁止（D-11）                   | settings 文件 `budget.totalS: 0` → 加载 WARN 一次、`/agent settings` 显示 `budget.totalS 1800`；spawn 出的 run `hardDeadlineAt` 有值且到点照杀；编辑器输入 0 被拒（min 1）；`mergeBudget` 对任何层的 0/负/NaN 回退下一层                                | U(P0 config) + M                       |      |
| V14 | print 模式不挂                             | `pi -p "<触发一个 20s 默认预算 + 宽限的 subagent>"` 进程在 run 结束后正常退出；FakeClock 用例断言宽限/延长路径不新增 pending interval                                                                                                                   | U(A：`clock.pendingTimers` 断言) + M   |      |
| V15 | `/reload` 无泄漏                           | reload 后旧 stack 的 `sendDeadlineNotice` 不再触发；新 stack 通知正常                                                                                                                                                                                   | I(fleet-widget-lifecycle 同款) + M     |      |
| V16 | B1 迁移与 zero-hang 上界                   | P6' `hardDeadlineAt` 恒定、`deadlineAt` 单调不减且 ≤ H；P11 `graceUntil ≤ H`；P13 延长不动 `phaseEnteredAt/lastEventAt/enqueuedAt/hardDeadlineAt`；P14 宽限进入 effects 无 `cancel_signal`                                                              | U(A 属性)                              |      |
| V17 | CC4 绝对帽子只收紧                         | 带 `SpawnRequest.deadlineAt` 的 run：`hardDeadlineAt = min(公式, cap)`；延长被 `no_headroom` 拒                                                                                                                                                         | U(final deadline-cap)                  |      |
| V18 | `wait()` 默认与延长共存（RK-5）            | 无 `overtime` 的 run 裸 wait 基准仍是 `deadlineAt`（既有用例不变）；有 `overtime` 的 run 裸 wait 不提前 `wait_timeout`                                                                                                                                  | U(B)                                   |      |
| V19 | 槽位挤压（RK-4）                           | `concurrencyLimit=1`：A 宽限中持有 slot，B 排队 `queueWaitMs` 到点 → B `failed`/`queue_timeout`；A 不受影响直至 `graceUntil`；不出现死锁/泄漏（`pool.audit().leaked === []`）                                                                           | I + M                                  |      |
| V20 | workflow 子 run 恒硬顶（D-10/RK-12a）      | `SubagentWorkflow` 内 `agent()` 派生的子 run（`host.ts` 恒带 `budgetOverride.totalMs`）：`hardDeadlineAt === deadlineAt`，到点直接 `timed_out`，不进宽限、无 `subagent:timeout`；编排器 `hostCallMs` 语义不变                                           | U(final deadline-cap 显式预算用例) + M |      |
| V21 | 宽限中跨越 stack 重建（RK-12b）            | run 在宽限中时 `/reload`：旧 watchdog 仍在 `graceUntil` 杀掉（zero-hang 不因重建失效）；`subagent:timeout` 最多一条；新 stack 的 `extend_subagent_timeout` 对该 run 返回 `unknown_run` 错误（与 steer/abort 跨 reload 行为一致）                        | I + M                                  |      |
| V22 | `total_grace` × `stop_requested`（RK-12c） | 宽限中 abort：`abort_grace` 与 `total_grace` 谁先到谁杀，两种顺序终态均 `aborted`、`stopCause: user_stop`                                                                                                                                               | U(A 定向) + M                          |      |
| V23 | 显式 `timeout_s` 硬顶（BL-4/D-10）         | `Agent(timeout_s: 20)`：`hardDeadlineAt === enqueuedAt + 20s`；t=20s 直接 `timed_out`，无宽限无通知；此前调用 `extend_subagent_timeout` → `no_headroom` 的"explicit timeout is a hard cap"文案；`timeout_s: 0` 被 schema 拒                             | U(P0 agent-tool + B tool + final) + M  |      |
| V24 | `not_started` 拒绝（D-14）                 | 对 `queued`/`starting` 的 run 调用工具 → throw 文案含 "has not started running yet"；reducer 对 `queue_wait` 收 `deadline_extended` → `illegal`                                                                                                         | U(A 矩阵 + B tool) + M                 |      |

---

## 6. 提交计划（Conventional Commits）

| #   | 包    | message 草案                                                                                                 | 内容                                                                                                                                                                                                                                                                                                                     |
| --- | ----- | ------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | P0    | `feat(core): timeout grace/extension types, budget knobs, deadline helpers and totalMs>0 invariant`          | types/status/deadline/ports/settings/setting-specs + runner/query 存根 + index forwardQuery + rpc/server 注释 + P0 测试（deadline-grace、extend-settings、agent-config、settings-time-migration）+ 四处基线预置。body 注明"行为零变化（除 totalS=0 回退默认）；B1 迁移至 hardDeadlineAt；新 RunInput 尚未进 INPUT_KINDS" |
| 2   | P0    | `feat(tools)!: Agent/SubagentWorkflow timeout_ms → timeout_s (seconds; explicit timeout is a hard cap)`      | agent-tool.ts + workflow-tool.ts + agent-tool.test.ts。footer `BREAKING CHANGE: the Agent and SubagentWorkflow tools take timeout_s (seconds) instead of timeout_ms; runs with an explicit timeout are hard-capped and never enter the grace window.`                                                                    |
| 3   | A     | `feat(core): grace window and deadline_extended in the run state machine`                                    | state-machine.ts + core.test.ts（矩阵 168、P6 改写、P11–P14、timeout grace 套件）                                                                                                                                                                                                                                        |
| 4   | A     | `feat(runtime): total_grace watchdog timer, deadline-following prompt guard and conditional cancel`          | watchdog.ts + runner.ts（`extendDeadline` 实体、`guardUntil`、`fireDeadline` 回读条件 cancel）+ runtime/watchdog 测试                                                                                                                                                                                                    |
| 5   | B     | `feat(tools): extend_subagent_timeout tool and QueryService.extendTimeout`                                   | tool + query-service 实现（含 `wait()` RK-5）+ adapter `extendDeadline` 透传 + tool-scope + 对应测试                                                                                                                                                                                                                     |
| 6   | B     | `feat(delivery): subagent:timeout notice channel with ack/background suppression`                            | deadline-notice.ts + adapter `deadlineNoticeHandler` + notifier 回归 + 测试。body 引 D-4 说明为何不进 outbox、D-17 说明不复用 ackHold                                                                                                                                                                                    |
| 7   | C     | `feat(ui): remaining-deadline field, grace highlight and overtime lines in fleet widget and /agent status`   | fleet-panel/fleet-widget/status + 测试                                                                                                                                                                                                                                                                                   |
| 8   | final | `feat(stack): wire timeout grace notices, extension tool, budget policy and extend.* settings`               | stack.ts + index.ts + spawn-service.ts（`applyBudgetPolicy` + BL-5 终态重建）+ 集成测试 + deadline-cap/spawn-service/settings-editor 补测                                                                                                                                                                                |
| 9   | final | `docs(timeout-notify): readme timeout_s, changelog, AGENTS.md core/runtime notes, plan verification results` | README 两份 + CHANGELOG `Unreleased`：Added（宽限/工具/widget/settings）+ Changed（`timeout_s`、`budget.totalS` 必须 > 0）；AGENTS.md `src/core`/`src/runtime` 行补 "grace/extension"；arch.md 状态行改"已实施"；本文 §5 填真机结果                                                                                      |

约束：commit 3 与 4 可合并为一个（若 A 的开发者更愿意一次交付），但 5/6 保持分开——工具与通知是两条独立回滚面；commit 2 单独成枚（breaking，便于 CHANGELOG 生成器识别）。`lint-staged` 会整文件重新暂存，分组提交只能按整文件切（见项目记忆），上述切分已按文件对齐。

---

## 7. 范围外 / 留 v2

- `/agent extend` 人类命令（Q1）；`set_subagent_timeout` 绝对值工具（arch §4.2 备选）；`ExtendSource` 扩展 `"user"`/`"rpc"`（D-13）
- 子 run 宽限通知经 fabric 路由给父 run（Q3 / R-14）
- 启动相位（`resolve_config`/`session_create`/`extension_bind`/`queue_wait`）的宽限与延长（D-14 已论证不做）
- `get_subagent_result.wait_ms` / `bash_job.wait_ms` 改秒：与本特性无关的独立 model-facing 面，且 `DEFAULT_WAIT_MS` 刚交付（commit `e5d15dd`），另开 chore 统一（D-12 一致性的后续项，需用户拍板）
- `TERMINAL_STATUSES` 其余散落拷贝（`stack.ts`/`index.ts`/`commands/status.ts`/`mention/mention.ts`/`core/delivery-key.ts`/`ui/fleet-panel.ts`）收敛到 `core/status.ts`（D-15 后续 chore）
- agent-type frontmatter 解析 `budget:` 键（今天 `AgentTypeConfig.budgetOverride` 只能程序设置，S19）
- `feishu-notify` 对宽限事件的卡片（当前完成类卡片走 background-idle 门控，宽限通知是紧急单条，语义不同，不混入）
- RPC `budgetOverride` 白名单放开新三键 / 单位改秒（BL-2 / D-12 例外）
