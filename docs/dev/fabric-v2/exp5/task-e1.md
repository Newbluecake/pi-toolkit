# 任务：fabric v2 消息投递层实施方案

你在 `/tmp/exp5-repo` 仓库工作。这是 pi 编码 agent 的扩展 `pi-toolkit`，其中 `src/fabric/` 是现有的 v1 inter-agent 消息织网（`message_agent` 工具、按 agent 类型 frontmatter 的 `can_message` 路由、mailbox、per-link throttle 等）。

现在要做 **fabric v2**。请产出它的**消息投递层实施方案**。

## 需求概要（来自前序阶段的交接）

- **R1**：任意两个 agent 之间可点对点通信，也可一对多广播；
- **R2**：发送永不阻塞——发送方绝不等待对方回复。

前序阶段已与用户讨论并**拍板了若干关键设计决策**（涉及发送权限、广播语义、唤醒规则、看板范围、超限处理、消息类型等），本交接没有逐条展开。

## 可以请教前序阶段的参与者

前序阶段的参与者（label：`upstream-expert`）读过完整的设计讨论与评审记录，现已结束工作，但可以被唤醒请教。需要时可以随时请教：调用 `Agent` 工具，`subagent_type: "upstream-analyst"`，`resume: "upstream-expert"`，`description` 随意，`prompt` 写你的问题（可以一次问多个）。它会在**当轮直接返回答案**，不会打断你的工作。

## 方案必须覆盖

1. 消息模型：字段与 kind 列表；
2. 发送权限与路由规则；
3. 点对点与广播各自的投递方式（push / notify / pull 由什么决定）；
4. 接收方的唤醒规则（什么消息能让一个 agent 起一轮模型调用、能否复活已结束的 agent）；
5. 看板（共享公告）的存储方式与有效范围；
6. 配额 / 收件箱满时的处理；
7. 要改哪些文件与函数（基于现有 `src/fabric/` 代码）；
8. 测试要点。

## 信息来源限制

只允许读取 `/tmp/exp5-repo` 内的文件与你自己的任务目录。**不要**搜索或读取任何其它位置。

## 输出

把方案写到派单消息里指定的输出路径。除该输出文件外不要修改任何文件，不要改生产代码，不要运行 `git stash` 等破坏性命令。
完成后，最终回复只给一行完成确认 + 文件路径。
