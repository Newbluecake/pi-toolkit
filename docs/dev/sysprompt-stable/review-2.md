## 评审结论：有条件通过

v2 相对 v1 是一次真正的机制升级，不是换说法：

- 阻塞 #2（唤醒轮）**从根上消失**了。我验证了关键链条：`_preparePromptAndToolLoadout` 的 diff 输入是 `this.agent.state.messages` / `buildSessionProjection().messages`（`pc87:agent-session.js:1035`、`:388`），而 `emitContext` 在入口就 `structuredClone(messages)`（`pc87:runner.js:902`）——我们在请求时改的是克隆体，transcript 永远看不到我们的键，所以 v1 的 `null` 删除补丁在 v2 里**不可能产生**。这是结构性保证，不是约定。
- 阻塞 #1（版本溯源）已全面改写并加了「观测锁存 + legacy 退化」，<0.87 不丢内容。v2 引用的 11 条 pi 事实我逐条对了行号，**全部命中**（见已核查项）。

但有 1 条阻塞：方案的 D1 决策记录自相矛盾——它以「`{systemPrompt}` 是 run 级、唤醒轮拿不到」否决了 legacy 折叠路线，而方案自己的 S1 恰恰就是解决这一点的，并且 S1 被明确规定「主方案落地后**不删除**」。于是「legacy 折叠（今天的形态）+ 冻结快照 + 尾部更新消息 + S1 回放」这个能同时兑现 G1/G2/G3/G4 的方案从未出现在任何备选表里，而它比主方案少一整层（`projection.ts`、锁存、三态、`core-sections` 的 request-time 分支）。这不是口味问题：主方案的严重 #2、#3 与风险 R5/R6/R8 全部是投影层独有的，简方案里不存在。必须在 D1 里把它评估掉再动 M1/M2。

S1（§3.5）本身没有阻塞项，可以先行开工。

---

## 问题清单

| #   | 严重度   | 位置                                         | 问题（含证据）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | 建议                                                                                                                                                                                                                                                                                                                                                                                          |
| --- | -------- | -------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **阻塞** | §5 D1 备选表、§3.1、§3.2 三态表、§7.1        | **D1 否决 `{systemPrompt}` 的唯一理由被方案自己的 S1 推翻，且更简的替代方案未被评估。** D1 写「`{systemPrompt}`：run 级（§1.2），唤醒轮拿不到」——但 §3.5 的 S1 正是用 `context_with_system` 把用户轮的强制文本回放到唤醒轮，D9 还明说 S1「主方案落地后不删除」，D7 说 legacy 折叠与今天**逐字节相同**。把这三块拼起来就是一个从未列入备选的方案：**legacy 折叠 + 三段冻结快照 + 尾部更新消息 + S1 回放**。它同样兑现 G1（S1）、G2（快照）、G3（更新消息）、G4（同一套刷新点），却不需要 `projection.ts`、不需要 `globalThis` 锁存、不需要三态、不需要 request-time 的 provider 调用路径，并且**天然不存在**本表 #2（合成头）、#3（R5 静默丢失）、R6（每进程一次形态切换）、R8（token 估算偏差）这四个问题——因为开头形态与今天逐字节相同，且我们仍在 handler 折叠链里，后加载扩展读 `event.systemPrompt`（`pc87:runner.js:1031-1033` 的 getter，forced 后返回 forced 本身）会把我们的内容带上。公平起见，投影确有一项真实优势：强制文本在 run 内被 `prepareNextTurnWithContext` 原样沿用（`pc87:agent-session.js:391-401`，`normalizeBuildSystemPromptOptions` 复制 `forceSystemPrompt`，`pc87:system-prompt.js:12`），所以 run 内 pi 自己的 `tools` 说明段会滞后；投影模式下 pi 的那一半是实时的。但这是**今天已有**的行为，不是新增退化，用它换一整层机制是否划算，方案必须显式论证。 | 在 D1 备选表里新增一行「legacy 折叠 + 冻结快照 + S1 回放」，给出采纳或否决结论；若否决，用可验证的收益差（例如「run 内 pi 自身 section 滞后」的实际代价）说明，而不是沿用已被 S1 推翻的理由。若采纳，M1/M2/M3 的规模至少砍掉一半，§7.1 交付表需重排。                                                                                                                                         |
| 2   | 严重     | §4.2 `injectSections` 第 2 条契约            | **「`messages[0]` 不是 system ⇒ 在最前插入合成头」这一分支会做出两件方案自己要避免的事。** (a) 非 capable 路由走 `collapseSystemMessages`（`ai87:dist/utils/transcript.js:96-100`）→ `getCurrentSystemMessage` 按**消息顺序**建 sections Map（`:56-84`），合成头在 index 0 ⇒ **我们的三段排在 pi 的 `preamble` 之前**——正是 §4.2 用来否决「改 `content`」的那条理由。(b) capable 路由下 `resolveTranscript` 原样返回（`:102-103`），`anthropic-messages.js:790-800` 的 `buildParams(model, normalizedContext, …)` 取 `getInitialSystemMessage(context.messages)` 作为 `initialSystemText` 与 `initialTools` ⇒ **系统提示只剩我们三段、`initialTools = []`**。方案称「当前无 capable 模型」，但 `ai87:dist/types.d.ts:575` 原文是「the generated model catalog **enables it for capable models**」——这是一颗随 pi-ai 目录更新引爆的雷。该分支并非纯理论：`buildContextEntries` 在压缩后把 compaction 条目放 index 0（`pc87:session-manager.js:216`），只有当该条目带 `systemMessage` 快照时 `messages[0]` 才是 system（`:189`，快照写入见 `:882,895`）——**0.86 之前的 pi 写的 compaction 条目没有这个字段**，恢复这类会话即命中；另一个可达路径是「会话尚无任何用户轮就被通知唤醒」（bash job 恢复、调度器、/reload 后子 agent 完成，`src/stack.ts:412/960/973`）。                     | 删掉合成头。改为：注入**第一条 `role === "system"` 的消息**（这样我们的键仍排在该消息自身键之后，顺序正确）；若整条 transcript 一条 system 消息都没有，**放弃注入并记一条日志**，不要伪造开头。单测补两个夹具：`messages[0]` 非 system 但后面有 system；完全没有 system。                                                                                                                     |
| 3   | 严重     | §4.7 表最后一行、§8 R5                       | **R5 是「静默且全量」的内容丢失，兜底只有「当前用户环境没有这种扩展」。** 我复核了这一前提：`~/.pi/agent/settings.json` 的 `packages` = `["../../ai/pi-toolkit", "../../ai/pi-traffic-record"]`，加本地 `~/.pi/agent/extensions/orca-agent-status.ts`；`pi-traffic-record` 只注册 `session_start/model_select/turn_start/before_provider_headers/before_provider_request/after_provider_response/message_end/session_shutdown`，两者 `systemPrompt` 均 0 命中——**前提当下成立**。但这是环境快照，不是机制保证。机制上：任何在我们之后注册的 `before_agent_start` 只要返回 `{ systemPrompt }`，`pc87:runner.js:1043` 就写 `currentOptions.forceSystemPrompt`，`pc87:agent-session.js:1044-1060` 的投影在 `context_with_system` **之后**运行并整块覆盖开头（我已验证包裹关系：`transformContext` 在构造期只包一次，`:174`；内层 previous 即 `pc87:sdk.js:248-253` 的 `emitContext`）⇒ 我们注入的三段被丢弃，**用户轮没有、唤醒轮有**，比今天更糟且无任何信号。方案的检测只覆盖「更早的 handler」。                                                                                                                                                                                                                                                                                       | 加请求时检测：在 `context_with_system` 里比对 `ctx.getSystemPrompt()`（`pc87:agent-session.js:880` = `buildSystemPrompt(_runSystemPromptOptions ?? base)`，forced run 中即 forced 文本）与 `getSystemMessageText(getCurrentSystemMessage(event.messages))`；显著不一致 ⇒ 判定「本 run 存在我们未设置的强制文本」，记一次 WARN 并把本会话有效模式降为 legacy。至少要做到**响一声**，不能静默。 |
| 4   | 一般     | D4 `session_tree` 行、§4.4 钩子表            | **删掉 `session_tree` 刷新点后，`announced` 与分支上下文去同步。** `announced` 的语义是「模型上下文里已经有一条讲这个内容的更新消息」（I3 去重依据）。分支导航后上下文换成另一条路径（`pc87:session-manager.js:201-228` 按 `buildSessionPath` 重建），那条路径上可能根本没有这些更新消息，而 hub 仍认为已通知 ⇒ 模型停在旧快照，**且没有任何纠正**，直到下一个刷新点（可能整个会话都不到）。review-1 #6 给的理由是「刷新会打掉活缓存」，这条仍然成立——但结论应该是「不刷新快照」，不是「什么都不做」。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | `session_tree` 上不刷新快照，但把每段 `announced = snapshot`（忘掉「已通知」）。成本为零（不改开头字节），下一个用户轮若 `live ≠ snapshot` 会自动补发一条更新消息。property 测试加一条：随机序列里插入 `session_tree`，断言「有效视图最终收敛到 live」。                                                                                                                                      |
| 5   | 一般     | §3.5 S1 伪代码                               | **S1 有更简单的接缝，`pendingCapture` 是纯粹的多余状态。** `pc87:runner.js:1016-1059` 里 `currentOptions` 是同一个对象，`:1043` 写入 `forceSystemPrompt`，`pc87:agent-session.js:1319` 把它原样赋给 `_runSystemPromptOptions`，`:880` 再渲染回同一字符串（`buildSystemPromptState` 对 forced 直接返回 `{content: forced}`，`pc87:system-prompt.js:121-124`）。所以**在 `before_agent_start` 里直接 `captured = event.systemPromptOptions.forceSystemPrompt` 得到的字符串，与 S1 在请求时 `ctx.getSystemPrompt()` 拿到的逐字节相同**。延迟捕获唯一多买到的是「我们之后的 handler 又改了 forced」，而 §3.5 边界 3 已经明确放弃那种情形。代价却是实打实的：边界 4（`prompt()` 在 hook 之后抛错 ⇒ `pendingCapture` 悬挂 ⇒ 下一个唤醒轮把 base 文本当成 forced 捕获并长期回放）就是这条多余状态造成的。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | S1 改为单状态机：`before_agent_start` 里 `captured = event.systemPromptOptions.forceSystemPrompt`（`undefined` 即清空）；删除 `pendingCapture` 与边界 4。`context_with_system` 只剩「有 captured 就回放」。测试相应简化。                                                                                                                                                                     |
| 6   | 一般     | §4.6「过渡 run 内的安全性」、§4.4 钩子表     | **锁存翻转与 LegacyReplay 的交接没有定义。** 方案说进程第一个用户轮以 legacy 跑、其首个请求触发锁存、「锁存后同一请求就按投影注入」。那么这一个请求里：legacy 分支已在 `before_agent_start` 里 arm 了 LegacyReplay 的捕获，而 `context_with_system` 因为刚刚锁存已切到 stable 分支、不再消费捕获 ⇒ 捕获状态悬挂（与 #5 同源）。另外「有效模式」在一次 `emitContext` 内部翻转，意味着同一个 run 的第 1 个和第 2 个请求走不同分支，而 §3.2 的三态表是按「模式」而不是按「run」描述行为的，实现者无从判断该在哪里读锁存。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | 明确规定：**有效模式在 `before_agent_start`（或 run 的第一个 `context_with_system`）时点冻结，整个 run 不变**；锁存在请求里写入，但只影响**下一个** run 的模式解析。这样过渡是 run 粒度的，LegacyReplay 与投影不会在同一 run 内交错。§3.2 表加一列「模式解析时点」。                                                                                                                          |
| 7   | 一般     | §8 R8                                        | **R8 的事实写反了。** `pc87:agent-session.js:3098-3131` 的 `getContextUsage` 走 `estimateProjectedContextTokens`（`pc87:dist/core/compaction/compaction.js:158-189`）：只要最近一次压缩之后存在带 usage 的 assistant 消息，它就**返回以 provider 实际上报 input tokens 为锚的 `estimate`**（`:159-181`），而真实请求里是**包含**我们注入的三段的 ⇒ 常态下三段已被计入。只有「压缩后尚无 assistant usage」的回落分支（`:183-189`）才是纯估算并漏掉我们。仓库有 5 个消费者（`src/stack.ts:572` 的 compact-hint、`src/tools/compact-tool.ts:146`、`switch-context-tool.ts:159`、`set-compact-threshold-tool.ts:86`、`src/hud/footer.ts:256`），AGENTS.md 里 compact-hint 的 force 线是贴着 pi 自己的保留线调的，一条写反的风险描述会误导后人去"补偿"一个不存在的偏差。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | R8 改写为：「常态（usage-anchored）下三段已计入；仅在最近一次压缩后还没有 assistant usage 的窗口内漏计，该窗口 `getContextUsage()` 本来就返回 `tokens: null`（`agent-session.js:3120`），无实际影响。」并删掉「阈值压缩略晚触发数 KB」的推论。                                                                                                                                                |
| 8   | 一般     | §4.4 钩子表「子会话」列、D6                  | **子会话不注册 `session_compact`，长 subagent 会话压缩后快照永不刷新。** 子会话不是都很短：`src/runtime/` 的 runner 支持长任务与多轮，pi 的轮中阈值压缩对子会话同样生效（`pc87:agent-session.js:2152-2231`）。压缩之后子会话的整段上下文是新字节，本来就是**免费刷新点**，此时不刷新反而把旧快照一直带下去。理由「子会话记忆默认只读、快照几乎不变」只对记忆成立，不构成不注册的理由——注册成本是一行。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | 子会话的钩子集合加上 `session_compact`（`model_select`、`session_tree` 仍可省）。§4.4 表与 D6 同步。                                                                                                                                                                                                                                                                                          |
| 9   | 一般     | §7.5 检查清单 C3–C7                          | **清单的工作量估计与仓库实际不符，且漏了一条 0.86 Breaking。** 我在仓库里 grep：`shouldStopAfterTurn` 0 处、`agent.state.messages` 赋值 0 处、`pi.on("context"` 0 处、`ExtensionEvent` 0 处、手工构造 `type: "turn_end"` 事件 0 处（6 处 `pi.on("turn_end")` 都只用 `_event`）。所以 C3/C4/C6/C7 基本是"复核"而非改动；真正会让 typecheck 红的是 C5（`ToolResultMessage` 变条件类型、`JsonValue` 只读数组——仓库大量工具返回 `details`）与 C9（`pi.on()` 返回值变化，若有 `void` 上下文约束）。另外 0.86.0 的第 1 条 Breaking「pi-ai provider stream 输入 `Context` → `TranscriptContext`」未列（仓库不注册自定义 provider，实际 N/A），但清单自称逐条，缺项会让复核者以为漏看。0.85.x 无 Breaking Changes 段（已核 CHANGELOG），这一点方案隐含正确。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | 按实际 grep 结果重排 C3–C7 的优先级（C5/C9 置顶、其余标「验证性」），补一行 0.86.0 provider 输入 Breaking 并标 N/A + 依据，并注明「0.85.x 无 Breaking」。                                                                                                                                                                                                                                     |
| 10  | 建议     | §4.4 hub 契约                                | **「provider 必须同步且有界」应写成不变量，而不是类型注释。** `context_with_system` 的 handler 被 `await`（`pc87:runner.js:935`），事件里**没有 `signal`**，pi 侧也没有超时。一个阻塞或返回 Promise 的 provider 会把**每一个 LLM 请求**挂住——这直接踩 AGENTS.md 的零挂死红线。当前两段内容的计算确实是同步的（`src/memory/render.ts:83` `memoryFingerprint`、`:140` `renderMemoryBlock` 均为同步函数），所以契约可满足，但没有护栏。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | 把「provider 同步、无 IO 等待、有界」升格为 I7，并在 `register()` 里对 `provider` 返回 `Promise`（`typeof x?.then === "function"`）直接判 SKIP + 记日志；加一条单测。                                                                                                                                                                                                                         |
| 11  | 建议     | §4.6 锁存                                    | **`globalThis[Symbol.for("pi-subagent:ctx-with-system")]` 没有 reset 出口，会污染 vitest。** §7.3 的 hub 测试要断言「未锁存 ⇒ legacy」和「锁存后 stable」，两者在同一进程里按文件顺序跑，先锁存的用例会让后面的用例失效（且不稳定复现）。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `pi-compat.ts` 的访问器同时导出一个 `__resetProjectionLatchForTests()`（命名带 `ForTests`），在 hub 测试的 `beforeEach` 调用。                                                                                                                                                                                                                                                                |
| 12  | 建议     | §4.5 memory registration                     | **cwd 回退从三级降为一级，丢了一条防御。** 今天是 `event.systemPromptOptions.cwd ?? ctx.cwd ?? process.cwd()`（`src/memory/inject.ts:58`），方案改为 `ctx.cwd`。`ctx.cwd` 是带 `assertActive()` 的 getter（`pc87:runner.js:565-568`），runner 失效时**会抛**，此时 hub 兜底为 SKIP ⇒ 记忆段缺席；今天则回落到 `process.cwd()` 仍能渲染。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | provider 里保留 `try { ctx.cwd } catch {} ?? process.cwd()` 的两级回退，并在注释里写明 `assertActive` 这条原因。                                                                                                                                                                                                                                                                              |
| 13  | 建议     | §4.6 末段 / §7.5 末段（U1 = C 分支）         | **「命名空间导入 + `typeof` 探测」在 0.84.4 的**类型**下仍然编译不过。** 我确认 `ai84:dist/index.d.ts` 不导出任何 transcript 工具（grep `transcript`/`getSystemMessageText` 0 命中），因此 `ns.getCurrentSystemMessage` 是 TS2339，`typeof` 也救不了。方案原话「命名空间导入不会因缺名导致模块链接失败」说的是运行期，成立；编译期不成立。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | 在 §4.6 里写明需要一次显式 `as unknown as { getCurrentSystemMessage?: (...) => ... }` 的窄化，并把它集中在 `pi-compat.ts` 一处（符合该文件「唯一接触未文档化形态」的定位）。                                                                                                                                                                                                                  |
| 14  | 建议     | §7.3 `projection.test.ts` 的 foldLegacy 断言 | **legacy 回归 oracle 覆盖不全。** 以三个 `append*` 为 oracle 只钉住了「单段拼接」，没钉住 legacy 分支必须复现的另外两条现状语义：① 顺序必须是 记忆 → 类型 → 模型（今天由 pre-guard/post-guard 注册顺序决定，`src/memory/index.ts:50-58` vs `src/index.ts:394`）；② 三段都无内容时必须返回 `undefined` 而不是 `{ systemPrompt: 原串 }`（今天 `src/index.ts:413` 用 identity 比较提前退出，`inject.ts:76` 提前 return）——否则会凭空多出一次 forced 投影路径。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | 在 foldLegacy 测试里显式加这两条断言：三段齐全时的完整串逐字节等于「现状三个 hook 串联」的结果；全空时 `foldLegacy` 返回与入参**同一引用**，hub 据此返回 `undefined`。                                                                                                                                                                                                                        |

---

## review-1 处置核验

| review-1 # | 原严重度 | v2 自述                | 复评判定             | 依据                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ---------- | -------- | ---------------------- | -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1          | 阻塞     | 采纳并扩展             | **已消解**           | 全文改 0.87.1 溯源，我抽查 11 条全部命中（见已核查项）。`sections` 在 v2 **完全不再依赖**：`injectSections` 直接写 `messages[0].sections`，不碰 `systemPromptOptions.sections`；`grep -n "systemPromptOptions" plan.md` 只剩 legacy 分支读 `forceSystemPrompt`。锁存 + legacy 退化在 `pc84` 上不丢内容（`pc84` 的 `pi.on` 存 handler 但从不分发，`pc84:…/loader.js:233-239`）。                                          |
| 2          | 阻塞     | 采纳，改机制           | **已消解（结构性）** | 唤醒路径确认：`pc87:agent-session.js:2401-2404` `triggerTurn` ⇒ `_runAgentPrompt`，全文件 `emitBeforeAgentStart` 仅 `:1283` 一处。v2 不再写 section ⇒ `_preparePromptAndToolLoadout` 的 diff 输入（`:1035` `agent.state.messages`、`:388` `buildSessionProjection`）里没有我们的键 ⇒ 不产生 `null` 补丁。且 `emitContext` 入口 `structuredClone`（`runner.js:902`）保证我们改不到 transcript。**是真消解，不是换说法。** |
| 3          | 严重     | 采纳（I5）             | 已消解（文本层面）   | §4.1 I5 + §4.4 末段 + §4.7 表方向一致，不再自相矛盾。落地正确性依赖本表 #2 的头插分支修掉。                                                                                                                                                                                                                                                                                                                              |
| 4          | 严重     | 采纳（SKIP）           | 已消解               | 状态机 `refresh` 分支对 SKIP 「不落快照、不清 stale」，I4 与 §7.3 的首轮 SKIP 用例对齐。副作用（持续 SKIP ⇒ 每请求重试 provider）可接受，因 provider 同步有界（见 #10）。                                                                                                                                                                                                                                                |
| 5          | 严重     | 采纳（三态）           | 已消解               | `legacy` 保留 `append*` 作 oracle、与今天逐字节相同；同时兼作 <0.87 退化路径。测试覆盖面见本表 #14。                                                                                                                                                                                                                                                                                                                     |
| 6          | 一般     | 采纳                   | **部分消解**         | 逐事件对照表已写，`session_tree` 已删；但删除引入了 `announced` 去同步的新洞（本表 #4）。                                                                                                                                                                                                                                                                                                                                |
| 7          | 一般     | 采纳                   | 已消解               | 「伪抖动」论断与假锚点已删；`resolvePromptModels` 理由改为收敛来源（I7），合理。                                                                                                                                                                                                                                                                                                                                         |
| 8          | 一般     | 采纳并加强             | 已消解               | I2 限定到 `before_agent_start` 返回后；`session_compact` ⇒ stale ⇒ 下一个请求刷新，窗口确实缩到一个请求。我另确认扩展提供的压缩（`switch_context` 走 `session_before_compact` 返回 `{compaction}`）**同样**发 `session_compact`（`pc87:agent-session.js:1904-1907` 设 `fromExtension`，`:1938-1946` 照常 emit），F10 成立。                                                                                              |
| 9          | 建议     | 采纳（删）             | 已消解               | `parseSectionUpdate`/`hash`/`visible()`/`buildContextEntries` 依赖全部消失，`update-message.ts` 只剩渲染。                                                                                                                                                                                                                                                                                                               |
| 10         | 建议     | 已消解                 | 已消解               | 机制变更后该细节不存在。                                                                                                                                                                                                                                                                                                                                                                                                 |
| 11         | 一般     | 采纳                   | 已消解并复核         | 我重新 grep：`ai87:dist/models.generated.js` 中 `supportsMidConvoSystemMessages` **0 次**。但请注意 `ai87:dist/types.d.ts:575` 的注释声明目录"会为 capable 模型开启"——这正是本表 #2(b) 的引信，E2「收益为零」是当下结论，不是永久结论。                                                                                                                                                                                  |
| 12         | 一般     | 采纳（A1–A3 + T-REAL） | **已消解且可落地**   | T-REAL 依赖的两个导出均存在：`createAgentSession`（`pc87:dist/index.d.ts:19`）、faux provider（`ai87:dist/index.d.ts:23` `export * from "./providers/faux.ts"`，0.84.4 亦有）。断言用的 `resolveTranscript` / `getSystemMessageText` 也从 `ai87` 包入口导出（`dist/index.d.ts:32-33`）。A1–A3 的通过条件可判定。                                                                                                         |
| 13         | 建议     | 采纳                   | **部分消解**         | ① `systemPrompt: { mode }` 子对象与 `cacheTtl.mode` 同构，合理（`src/config/settings.ts:330,440,685,756` 为先例）。② 子会话钩子收敛，但收敛过头，漏了 `session_compact`（本表 #8）。                                                                                                                                                                                                                                     |

---

## 已核查项

### pi 0.87.1 源码（全部实测，行号为本机实际值）

| 方案论断                                                                   | 判定                           | 证据                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| -------------------------------------------------------------------------- | ------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F5 `context_with_system` 每请求触发，在 `context` 之后、强制投影之前       | **成立**                       | 定义 `pc87:dist/core/extensions/types.d.ts:526-533`；注册重载 `:992`；分发 `pc87:dist/core/extensions/runner.js:930-958`（`context` 循环 `:903-928` 在前）；接线 `pc87:dist/core/sdk.js:248-253`；每请求调用 `core87:dist/agent-loop.js:262-264`（`streamAssistantResponse` 开头）。**投影包裹关系实测**：`pc87:agent-session.js:1044-1047` 取 `previousTransformContext`（即 sdk 的 `emitContext`）先调用、再套 forced head ⇒ `context_with_system` 确在强制投影之前。 |
| 投影只在构造期安装一次（不会随 run 叠加包裹）                              | **成立**（方案未提，但是前提） | `pc87:agent-session.js:174` 是 `_installAgentForcedPromptProjection()` 的唯一调用点。                                                                                                                                                                                                                                                                                                                                                                                   |
| handler 返回 `{ messages }` 原样生效；丢掉开头 system 会被报错但仍尊重     | **成立**                       | `pc87:runner.js:934-947`（`currentMessages = handlerResult?.messages ?? currentMessages`，随后 `emitError` 警告但不回滚）。                                                                                                                                                                                                                                                                                                                                             |
| 我们的注入改不到 transcript                                                | **成立**                       | `pc87:runner.js:902` `structuredClone(messages)`；`core87:agent-loop.js:263-264` 用返回值另起变量，`context.messages` 原数组保持不变。                                                                                                                                                                                                                                                                                                                                  |
| F6 `ctx.getSystemPrompt()` 在强制 run 中 = 强制文本                        | **成立**                       | `pc87:agent-session.js:878-881`（`buildSystemPrompt(_runSystemPromptOptions ?? _base)`）、`:2474`（暴露给 ctx）；`pc87:system-prompt.js:121-124` forced ⇒ `{content: forced}` 无 sections；`ai87:dist/utils/text.js:11-18` 渲染即原串。`getSystemPrompt` 在**普通** `ExtensionContext` 上（`types.d.ts:249`），非仅命令上下文。                                                                                                                                         |
| F8 `forceSystemPrompt` 对后续 handler 可见；`event.systemPrompt` 是 getter | **成立**                       | `pc87:runner.js:1016-1017`（同一 `currentOptions`）、`:1031-1033` getter、`:1043` 写入、`:1058` 返回。                                                                                                                                                                                                                                                                                                                                                                  |
| §1.2 唤醒轮不走 `before_agent_start`                                       | **成立**                       | `pc87:agent-session.js:2401-2404`（`triggerTurn` ⇒ `_runAgentPrompt`）；`emitBeforeAgentStart` 唯一调用点 `:1283`；`:1100` finally 清 `_runSystemPromptOptions`；`:391` `prepareNextTurnWithContext` 回落 base。                                                                                                                                                                                                                                                        |
| F11 `getSystemPromptOptions()` 仅命令上下文，返回 base                     | **成立**                       | `pc87:types.d.ts:255-257`；`pc87:runner.js:631-635`；`pc87:agent-session.js:2475` 返回 `_baseSystemPromptOptions`。                                                                                                                                                                                                                                                                                                                                                     |
| F1/F2 section 渲染形态与排序                                               | **成立**                       | `pc87:system-prompt.js:105-115`（`cwd` 后追加自定义、空串跳过、除 preamble 外全部 `<name>\n…\n</name>`）、`:7` 名称正则与 §4.2 `SECTION_NAME_RE` 一致；`ai87:transcript.js:56-84` `getCurrentSystemMessage` 按消息顺序建 Map（**这是本表 #2 的依据**）。                                                                                                                                                                                                                |
| F3 diff 语义（缺键 ⇒ `null`）                                              | **成立**                       | `pc87:system-prompt.js:134-146`；`pc87:agent-session.js:1035`。                                                                                                                                                                                                                                                                                                                                                                                                         |
| F4 无 capable 模型 / 折叠                                                  | **成立**                       | `grep -c supportsMidConvoSystemMessages ai87:dist/models.generated.js` = **0**；`ai87:transcript.js:96-103`；`ai87:dist/api/anthropic-messages.js:335` + `:383` `buildParams(model, normalizedContext, …)`。                                                                                                                                                                                                                                                            |
| F10 `session_compact` 手动/自动/扩展压缩均发出                             | **成立**                       | `pc87:agent-session.js:1938-1946`（手动，含 `fromExtension`）、`:2227`（自动）。                                                                                                                                                                                                                                                                                                                                                                                        |
| 压缩后 `messages[0]` 仍是 system（0.86+ 写的条目）                         | **成立，但有例外**             | `pc87:session-manager.js:216`（compaction 置 index 0）、`:189`（带 `systemMessage` 时它排在 summary 前）、`:882,895`（`appendCompaction` 写入快照，且快照取自 transcript ⇒ 不含我们的键）。**例外**：更老的 pi 写的 compaction 条目无该字段 ⇒ 命中本表 #2 的头插分支。                                                                                                                                                                                                  |
| `getContextUsage` 是 usage-anchored                                        | **成立（推翻 R8）**            | `pc87:agent-session.js:3098-3131`；`pc87:dist/core/compaction/compaction.js:158-189`。                                                                                                                                                                                                                                                                                                                                                                                  |
| pi 自己的 cache warmer 捕获的是 transform 之后的 context                   | **成立**                       | `core87:agent-loop.js:262-268`（transform → convertToLlm → normalizeContext → streamFunction），`pc87:sdk.js:241` 在 `streamFn` 内以该 context 调 `cacheWarmer.start`。⇒ 我们的注入会被 pi 的保活一并预热，与 §7.4 一致。                                                                                                                                                                                                                                               |
| 0.85.x 无 Breaking Changes；0.86.0/0.87.0 的 Breaking 清单                 | **核实**                       | `pc87:CHANGELOG.md` 中 `[0.85.0]`/`[0.85.1]` 无 Breaking 段；0.86.0 三条、0.87.0 五条，与 §7.5 对照后发现漏一条（本表 #9）。                                                                                                                                                                                                                                                                                                                                            |
| 0.84.4 缺 `context_with_system` / transcript 导出                          | **成立**                       | `grep context_with_system pc84:dist/core/extensions/types.d.ts` 无命中；`grep transcript ai84:dist/index.d.ts` 无命中；`pc84:…/loader.js:233-239` 的 `on()` 接受任意事件名、返回 `void`（**0.87 的 `:202-217` 返回 unsubscribe**，C9 相关）。                                                                                                                                                                                                                           |

### 仓库现状

- 现有注入点与 v2 §2 描述一致：`src/index.ts:394-414`（post-guard，`appendAgentTypes…` → `appendAvailableModels…` → `{ systemPrompt }`）、`src/memory/inject.ts:48-84`（pre-guard，含 `## Memory (<slug>)` 与 sentinel 双重去重、`catch` 永不崩）。
- 唤醒型 `sendMessage(triggerTurn: true)`：`src/stack.ts:321/412/960/973`（复核 review-1 结论）。
- provider 同步性：`src/memory/render.ts:83 memoryFingerprint` / `:140 renderMemoryBlock` 均为同步函数 ⇒ §4.4 的同步 provider 契约可满足。
- `ctx.getContextUsage()` 消费者 5 处（`src/stack.ts:572`、`src/tools/{compact,switch-context,set-compact-threshold}-tool.ts`、`src/hud/footer.ts:256`）；`getSystemPrompt` 消费者 0 处（仅测试 fake），R8 的后半句成立。
- 0.87 升级 fallout 实测：`shouldStopAfterTurn` / `agent.state.messages` 赋值 / `pi.on("context"` / `ExtensionEvent` / 手工 `type:"turn_end"` 事件夹具 —— **均 0 命中**（本表 #9 依据）。
- R5 前提：`~/.pi/agent/settings.json` `packages` = `["../../ai/pi-toolkit", "../../ai/pi-traffic-record"]` + `~/.pi/agent/extensions/orca-agent-status.ts`；`pi-traffic-record` 注册的 8 个事件里无 `before_agent_start`，两者 `systemPrompt` 0 命中 ⇒ **当下成立**。
- 设置形态先例：`src/config/settings.ts:330/440/685/756` 的 `cacheTtl` 子对象 + 非法值回落，§4.5 的 `systemPrompt: { mode }` 与之同构。

### 不变量检查

- **零挂死**：`before_agent_start` 返回 `{ message }` 仍是纯拉模式，不新起 run、不注册定时器 —— 成立。新增风险点只有 `context_with_system` 被 `await` 且无 signal（本表 #10），需要以「provider 同步有界」的不变量封死。
- **模块级可变状态**：hub 在 `activate()` 闭包内创建（§3.3），符合 /reload 约定；唯一的进程级状态是 `Symbol.for("pi-subagent:ctx-with-system")` 锁存，与现有 HOST_KEY 同约定，但缺测试 reset（本表 #11）。
- **I7（index.ts 只做装配）**：`core-sections.ts` + `resolvePromptModels` 把内联优先级逻辑移出 `src/index.ts:397-411`，方向正确。
- **子会话惰性**：hub pre-guard 创建、guard 处照常 return —— 成立；钩子集合收敛过头（本表 #8）。
- **cache-ttl 交互**：`before_provider_request`（`src/cache-ttl/cache-ttl.ts:301`）在 `onPayload` 阶段、`context_with_system` 之后，实测顺序为 transform → convertToLlm → streamFn → onPayload，两者不冲突；keepalive 回放的载荷已含我们的注入。G1 兑现后唤醒/用户两条前缀链合一，§7.4 第 1 段的收益论证成立。
- **过度设计**：本表 #1 是唯一的过度设计指控——投影层（`projection.ts` + 锁存 + 三态 + request-time provider 路径）是否必要，取决于 D1 对「legacy + 快照 + S1」的评估结论。除此之外 v2 相对 v1 已经**减法**（删 resync/hash/parse/visible/`session_tree`），方向正确。
